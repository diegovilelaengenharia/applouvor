---
phase: 02-confirmar-escala
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - admin/escalas.php
autonomous: true
requirements:
  - ESC-04

must_haves:
  truths:
    - "Cada card de escala na listagem admin/escalas.php exibe 'X/Y confirmados' onde X=confirmados e Y=total de participantes"
    - "Contador é visível para todos os usuários na listagem de escalas futuras"
    - "Contador é calculado a partir do participantsMap já carregado, sem query adicional"
  artifacts:
    - path: "admin/escalas.php"
      provides: "Contador de confirmações nos cards de escala"
      contains: "confirmados"
  key_links:
    - from: "admin/escalas.php (loop de escalas)"
      to: "$participantsMap[$schedule['id']]"
      via: "PHP loop contando status === 'confirmed'"
      pattern: "confirmados"
---

<objective>
Adicionar contador "X/Y confirmados" em cada card de escala na listagem `admin/escalas.php`. O líder consegue ver de relance quantos músicos confirmaram presença em cada escala sem precisar abrir o detalhe.

Purpose: Entregar ESC-04 — líder vê quantos confirmaram vs pendente vs recusaram. Este plano é independente do 2A/2B e pode executar em Wave 1 paralelo.
Output: Badge "X/Y confirmados" visível em cada card de escala na listagem.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-confirmar-escala/02-CONTEXT.md

<interfaces>
<!-- Contratos existentes que o executor deve usar diretamente. -->

De admin/escalas.php (já carregado na query de participantes, ~linha 53-64):
```php
// $participantsMap[$scheduleId] = array de participantes com:
// - $row['user_id']
// - $row['name']
// - $row['status'] — string: 'confirmed', 'pending', 'declined', 'absent'
// - $row['schedule_id']
```

No loop de renderização de escalas futuras (~linha 166+):
```php
$parts = $participantsMap[$schedule['id']] ?? [];
// $parts é um array; count($parts) = total de participantes
// Para contar confirmados: filtrar onde $p['status'] === 'confirmed'
```

Localização exata para inserir o contador no HTML (~linha 236):
```php
// Linha atual com "X músicas":
<span style="font-size: 0.75rem; ..."><?= $songsCount ?> músicas</span>
```
O contador de confirmações deve aparecer próximo a este elemento.

Padrão de badge já existente no arquivo (~linha 211):
```php
<span class="pib-badge <?= $myStatus == 'confirmed' ? 'pib-badge-success' : ... ?>">
    <?= $myStatus == 'confirmed' ? 'Confirmado' : ... ?>
</span>
```
Usar `pib-badge pib-badge-success` para o contador quando todos confirmaram, `pib-badge-warning` quando parcial, sem badge especial quando 0/N.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Calcular e exibir contador X/Y confirmados nos cards de escala</name>
  <files>admin/escalas.php</files>

  <read_first>
    - admin/escalas.php — ler a seção do loop de renderização de escalas futuras (~linhas 160-250) para entender onde inserir o contador. Localizar: `$parts = $participantsMap[$schedule['id']] ?? [];` e a linha com `$songsCount` músicas.
  </read_first>

  <action>
**Parte 1 — Calcular o contador dentro do loop de escalas futuras:**

Dentro do `foreach ($futureSchedules as $schedule)`, APÓS a linha `$isMine = $mySchedulesMap[$schedule['id']] ?? false;` (e a lógica de `$myStatus`), adicionar o cálculo:

```php
// Contador de confirmações (per D-09)
$confirmedCount = 0;
$totalParticipants = 0;
if (isset($participantsMap[$schedule['id']])) {
    $totalParticipants = count($participantsMap[$schedule['id']]);
    foreach ($participantsMap[$schedule['id']] as $p) {
        if ($p['status'] === 'confirmed') $confirmedCount++;
    }
}
```

**Parte 2 — Exibir o contador no HTML do card:**

Localizar a linha com `$songsCount` músicas dentro do card de escala:
```html
<span style="font-size: 0.75rem; color: var(--color-text-muted); font-weight: 600;"><?= $songsCount ?> músicas</span>
```

Após essa linha (ainda dentro do `<div style="display: flex; align-items: center; gap: 8px;">` que contém os avatares e o contagem de músicas), adicionar o contador:

```php
<?php if ($totalParticipants > 0): ?>
<span class="pib-badge <?= $confirmedCount === $totalParticipants ? 'pib-badge-success' : ($confirmedCount > 0 ? 'pib-badge-warning' : '') ?>" style="font-size: 0.6rem; padding: 2px 8px; font-weight: 800;">
    <?= $confirmedCount ?>/<?= $totalParticipants ?> confirmados
</span>
<?php endif; ?>
```

**Parte 3 — Replicar o mesmo cálculo no loop de escalas passadas:**

O arquivo também tem um loop para `$pastSchedules`. Aplicar o mesmo cálculo de `$confirmedCount` / `$totalParticipants` e exibir o badge no loop de escalas passadas (localizar loop `foreach ($pastSchedules as $schedule)` e adicionar o mesmo trecho PHP).
  </action>

  <verify>
    <automated>grep -c "confirmados" "admin/escalas.php"</automated>
  </verify>

  <acceptance_criteria>
    - `admin/escalas.php` contém `$confirmedCount` calculado por loop sobre `$participantsMap[$schedule['id']]`
    - Contém `$p['status'] === 'confirmed'` para o filtro de contagem
    - Contém o texto `confirmados` no HTML renderizado (pelo menos 1 ocorrência)
    - Contém `$confirmedCount === $totalParticipants ? 'pib-badge-success'` para coloração dinâmica
    - `php -l admin/escalas.php` sem erros de sintaxe
    - Cálculo usa apenas `$participantsMap` já disponível — sem query SQL adicional no loop
  </acceptance_criteria>

  <done>
Badge "X/Y confirmados" visível em todos os cards de escala com participantes. Fica verde quando todos confirmaram, amarelo quando parcial, sem cor quando nenhum confirmou. Líder vê situação de confirmação na listagem.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → PHP view | Dados lidos do banco, exibidos somente — nenhuma entrada de usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02C-01 | Information Disclosure | admin/escalas.php (contador) | accept | Contador de confirmações visível a todos os autenticados — intencional para coordenação do time |
| T-02C-02 | Denial of Service | admin/escalas.php | accept | Cálculo é PHP puro sobre array já carregado — zero queries extras, impacto mínimo |
</threat_model>

<verification>
1. `php -l admin/escalas.php` — sem erros de sintaxe
2. `grep "confirmados" admin/escalas.php` — linha encontrada
3. `grep "confirmedCount" admin/escalas.php` — pelo menos 2 ocorrências (cálculo + exibição)
4. `grep "pib-badge-success.*confirmed\|confirmed.*pib-badge-success" admin/escalas.php` — lógica de cor presente
5. Testar: escala com 3 participantes (2 confirmed, 1 pending) deve mostrar badge "2/3 confirmados"
</verification>

<success_criteria>
- Cada escala com participantes mostra badge "X/Y confirmados" no card da listagem
- Badge verde quando todos confirmaram, amarelo quando parcial
- Sem query SQL adicional (usa participantsMap já carregado)
- Sem regressão nos filtros de escala existentes (mine, type)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-confirmar-escala/02-03-SUMMARY.md`
</output>
