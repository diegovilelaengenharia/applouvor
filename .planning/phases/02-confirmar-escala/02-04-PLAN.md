---
phase: 02-confirmar-escala
plan: "04"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - includes/web_push_helper.php
  - api/send_reminders.php
  - admin/escala_detalhe.php
  - admin/index.php
autonomous: true
requirements:
  - ESC-05

must_haves:
  truths:
    - "Botão 'Lembrar quem não confirmou' aparece no dashboard do líder para escalas nos próximos 2 dias"
    - "Clicar no botão chama api/send_reminders.php que envia push para participantes com status pending"
    - "Push notification real (AESGCM) entregue ao browser dos músicos com payload JSON"
    - "Se AESGCM falhar, botão manual ainda funciona e erro é logado — valor mínimo garantido (D-02)"
    - "Ao salvar escala com sucesso, push de convocação é disparado automaticamente para os participantes (D-03)"
  artifacts:
    - path: "api/send_reminders.php"
      provides: "Endpoint para envio de lembretes push para pending users"
      contains: "send_reminders"
    - path: "includes/web_push_helper.php"
      provides: "Criptografia AESGCM real para payload de push"
      contains: "openssl_encrypt"
    - path: "admin/index.php"
      provides: "Botão manual de lembrete no dashboard do líder"
      contains: "send_reminders"
    - path: "admin/escala_detalhe.php"
      provides: "Trigger automático de push ao salvar escala"
      contains: "WebPushHelper"
  key_links:
    - from: "admin/index.php (botão lembrete)"
      to: "api/send_reminders.php"
      via: "fetch('../api/send_reminders.php')"
      pattern: "send_reminders"
    - from: "api/send_reminders.php"
      to: "includes/web_push_helper.php (WebPushHelper)"
      via: "require_once + new WebPushHelper()"
      pattern: "WebPushHelper"
    - from: "admin/escala_detalhe.php (save_changes handler)"
      to: "includes/web_push_helper.php"
      via: "require_once + WebPushHelper após $pdo->commit()"
      pattern: "WebPushHelper"
---

<objective>
Implementar push notifications reais para o fluxo de confirmação de escala:
1. Corrigir `includes/web_push_helper.php` com AESGCM real em PHP puro (openssl_encrypt + ECDH P-256 + HKDF)
2. Criar `api/send_reminders.php` — envia push para participantes com status pending
3. Adicionar botão "Lembrar quem não confirmou" no dashboard do líder (D-04)
4. Trigger automático ao salvar escala em `escala_detalhe.php` (D-03)

Purpose: Entregar ESC-05 — notificação push para músicos pendentes. Prioridade: botão manual primeiro (garantia), depois AESGCM, depois trigger automático.
Output: Push funcional end-to-end. Fallback: botão manual funciona mesmo se AESGCM falhar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-confirmar-escala/02-CONTEXT.md
@.planning/phases/02-confirmar-escala/02-01-SUMMARY.md

<interfaces>
<!-- Contratos existentes relevantes para este plano -->

De database/setup_push_notifications.sql:
```sql
-- Tabela push_subscriptions:
-- id, user_id, endpoint TEXT, p256dh VARCHAR(255), auth VARCHAR(255), created_at
```

De includes/config.php (padrão de carregamento de constantes):
```php
define('APP_ENV', ...);  // padrão existente
// VAPID keys devem ser carregadas da mesma forma:
// define('VAPID_PUBLIC_KEY', getenv('VAPID_PUBLIC_KEY') ?: '');
// define('VAPID_PRIVATE_KEY', getenv('VAPID_PRIVATE_KEY') ?: '');
// Chaves estão em maintenance/vapid_config.php (gitignored) em produção
// Em local: vir do .env
```

De includes/web_push_helper.php (estado atual):
- Classe `WebPushHelper($publicKey, $privateKey, $subject)`
- Método `sendNotification($subscription, $payload)` — $subscription = ['endpoint', 'p256dh', 'auth']
- Método `encrypt()` — MOCK vazio, precisa ser implementado
- VAPID JWT geração e assinatura ECDSA já implementados e presumivelmente corretos

De admin/escala_detalhe.php (save_changes handler, ~linha 79-115):
```php
if (isset($_POST['save_changes']) && $_SESSION['user_role'] === 'admin') {
    try {
        $pdo->beginTransaction();
        // ... atualizações de schedule, members, songs ...
        $pdo->commit();
        header("Location: escala_detalhe.php?id=$id&success=1");
        exit;
    } catch (Exception $e) {
        $pdo->rollBack();
        die($e->getMessage());
    }
}
```
O trigger de push deve ser inserido APÓS `$pdo->commit()` e ANTES de `header()`.

De admin/dashboard_data.php (dados disponíveis no dashboard):
- `$nextSchedule` — próxima escala do usuário logado
- Para o botão de lembrete precisamos de: escalas nos próximos 2 dias com participantes pending
- O botão deve ser visível apenas para admin

De includes/dashboard_render.php / dashboard_cards.php:
- Funções renderDashboardCard() — usar padrão existente para o widget de lembrete
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir AESGCM em web_push_helper.php (D-01 + fallback D-02)</name>
  <files>includes/web_push_helper.php</files>

  <read_first>
    - includes/web_push_helper.php — ler COMPLETO para entender a estrutura atual. Focar no método `encrypt()` (mock) e `sendRequest()` (envia payload vazio). A VAPID signing já está implementada — não modificar.
    - includes/config.php — verificar padrão de carregamento de constantes para saber como carregar VAPID_PUBLIC_KEY e VAPID_PRIVATE_KEY
  </read_first>

  <action>
**Implementação AESGCM em PHP puro** (substitui o método `encrypt()` e atualiza `sendRequest()`):

O método `encrypt($payload, $userPublicKey, $userAuthToken)` deve ser substituído pela implementação real abaixo. Atenção: preservar todos os outros métodos (`generateVapidHeaders`, `generateJWT`, `sign`, `signatureDerToRaw`, `adjustBigInt`, `base64UrlEncode`, `sendRequest`) sem modificações, exceto `sendRequest` que precisa enviar o payload criptografado corretamente.

**Substituir o método `encrypt()` pelo seguinte:**

```php
private function encrypt($payload, $userPublicKeyBase64, $userAuthTokenBase64) {
    // Decodificar chaves do subscriber
    $userPublicKey = base64_decode(strtr($userPublicKeyBase64, '-_', '+/'));
    $userAuthToken = base64_decode(strtr($userAuthTokenBase64, '-_', '+/'));

    // Gerar salt aleatório de 16 bytes
    $salt = random_bytes(16);

    // Gerar par de chaves ECDH efêmero (P-256)
    $localKeyPair = openssl_pkey_new([
        'curve_name' => 'prime256v1',
        'private_key_type' => OPENSSL_KEYTYPE_EC,
    ]);
    if (!$localKeyPair) {
        error_log('WebPush: openssl_pkey_new falhou: ' . openssl_error_string());
        return null;
    }

    // Extrair chave pública efêmera no formato uncompressed (0x04 + X + Y = 65 bytes)
    $localKeyDetails = openssl_pkey_get_details($localKeyPair);
    $localPublicKeyRaw = "\x04" . str_pad($localKeyDetails['ec']['x'], 32, "\x00", STR_PAD_LEFT)
                                 . str_pad($localKeyDetails['ec']['y'], 32, "\x00", STR_PAD_LEFT);

    // Carregar chave pública do subscriber (formato uncompressed P-256)
    $pem = $this->rawPublicKeyToPem($userPublicKey);
    $subscriberKey = openssl_pkey_get_public($pem);
    if (!$subscriberKey) {
        error_log('WebPush: falha ao carregar chave pública do subscriber: ' . openssl_error_string());
        return null;
    }

    // Shared secret via ECDH
    $sharedSecret = '';
    if (!openssl_pkey_derive($sharedSecret, $localKeyPair, $subscriberKey)) {
        error_log('WebPush: ECDH derive falhou: ' . openssl_error_string());
        return null;
    }

    // HKDF para derivar chave de criptografia e nonce
    // PRK = HMAC-SHA256(auth || \x00, sharedSecret || localPubKey || userPubKey)
    $prk = $this->hkdfExtract($userAuthToken, $sharedSecret);

    // CEK (Content Encryption Key) 16 bytes
    $cekInfo = "Content-Encoding: aesgcm\x00";
    $cek = $this->hkdfExpand($prk, $salt . $localPublicKeyRaw . $userPublicKey . $cekInfo, 16);

    // NONCE 12 bytes
    $nonceInfo = "Content-Encoding: nonce\x00";
    $nonce = $this->hkdfExpand($prk, $salt . $localPublicKeyRaw . $userPublicKey . $nonceInfo, 12);

    // Padding: 2 bytes de padding length (0) + payload
    $paddedPayload = "\x00\x00" . $payload;

    // Criptografar com AES-128-GCM
    $tag = '';
    $ciphertext = openssl_encrypt(
        $paddedPayload,
        'aes-128-gcm',
        $cek,
        OPENSSL_RAW_DATA,
        $nonce,
        $tag
    );

    if ($ciphertext === false) {
        error_log('WebPush: openssl_encrypt falhou: ' . openssl_error_string());
        return null;
    }

    return [
        'ciphertext'    => $ciphertext . $tag,
        'salt'          => base64_encode($salt),
        'localPublicKey' => base64_encode($localPublicKeyRaw),
    ];
}

private function rawPublicKeyToPem($rawKey) {
    // Encode P-256 public key (65 bytes uncompressed) para PEM SubjectPublicKeyInfo
    $header = "\x30\x59\x30\x13\x06\x07\x2a\x86\x48\xce\x3d\x02\x01"
            . "\x06\x08\x2a\x86\x48\xce\x3d\x03\x01\x07\x03\x42\x00";
    $der = $header . $rawKey;
    return "-----BEGIN PUBLIC KEY-----\n"
         . chunk_split(base64_encode($der), 64, "\n")
         . "-----END PUBLIC KEY-----\n";
}

private function hkdfExtract($salt, $inputKeyingMaterial) {
    return hash_hmac('sha256', $inputKeyingMaterial, $salt, true);
}

private function hkdfExpand($prk, $info, $length) {
    $output = '';
    $t = '';
    $counter = 1;
    while (strlen($output) < $length) {
        $t = hash_hmac('sha256', $t . $info . chr($counter), $prk, true);
        $output .= $t;
        $counter++;
    }
    return substr($output, 0, $length);
}
```

**Atualizar `sendRequest()` para enviar o payload criptografado:**

Localizar o método `sendRequest($endpoint, $encrypted, $headers)` e substituir o bloco de configuração do curl para:

```php
private function sendRequest($endpoint, $encrypted, $headers) {
    $ch = curl_init($endpoint);

    $headersList = [
        'Authorization: ' . $headers['Authorization'],
        'TTL: 2419200',
    ];

    $postBody = null;

    if ($encrypted !== null && !empty($encrypted['ciphertext'])) {
        $headersList[] = 'Content-Type: application/octet-stream';
        $headersList[] = 'Content-Encoding: aesgcm';
        $headersList[] = 'Encryption: salt=' . $encrypted['salt'];
        $headersList[] = 'Crypto-Key: dh=' . rtrim(strtr(base64_encode(base64_decode($encrypted['localPublicKey'])), '+/', '-_'), '=')
                        . ';p256ecdsa=' . $this->vapidPublicKey;
        $postBody = $encrypted['ciphertext'];
    }

    curl_setopt_array($ch, [
        CURLOPT_POST          => true,
        CURLOPT_POSTFIELDS    => $postBody,
        CURLOPT_HTTPHEADER    => $headersList,
        CURLOPT_RETURNTRANSFER => true,
        CURLOPT_SSL_VERIFYPEER => true,
    ]);

    $result   = curl_exec($ch);
    $httpCode = curl_getinfo($ch, CURLINFO_HTTP_CODE);
    curl_close($ch);

    if ($httpCode >= 400) {
        error_log("WebPush Error $httpCode for $endpoint: $result");
        return false;
    }

    return $httpCode >= 200 && $httpCode < 300;
}
```

**Atualizar `sendNotification()` para lidar com falha de encrypt (D-02 fallback):**

No método `sendNotification()`, substituir `$encrypted = $this->encrypt(...)` por:

```php
$encrypted = $this->encrypt($payloadJson, $p256dh, $auth);
// Fallback D-02: se encrypt falhar, ainda tenta enviar sem payload
// (notificação genérica sem título/corpo, mas entregue)
if ($encrypted === null) {
    error_log('WebPush: AESGCM falhou, enviando sem payload para ' . $endpoint);
    $encrypted = ['ciphertext' => '', 'salt' => '', 'localPublicKey' => ''];
}
```
  </action>

  <verify>
    <automated>grep -c "openssl_encrypt" "includes/web_push_helper.php"</automated>
  </verify>

  <acceptance_criteria>
    - `includes/web_push_helper.php` contém `openssl_encrypt` com `'aes-128-gcm'`
    - Contém `openssl_pkey_new` com `'curve_name' => 'prime256v1'`
    - Contém `openssl_pkey_derive` para ECDH
    - Contém `hkdfExtract` e `hkdfExpand` como métodos private
    - Contém `rawPublicKeyToPem` como método private
    - Contém `"Content-Encoding: aesgcm"` no sendRequest
    - Contém `Crypto-Key: dh=` no sendRequest
    - Contém o fallback `if ($encrypted === null)` em sendNotification
    - `php -l includes/web_push_helper.php` sem erros de sintaxe
    - Os métodos originais `generateVapidHeaders`, `generateJWT`, `sign`, `signatureDerToRaw`, `adjustBigInt`, `base64UrlEncode` estão preservados
  </acceptance_criteria>

  <done>AESGCM real implementado em PHP puro. Fallback para envio sem payload se criptografia falhar.</done>
</task>

<task type="auto">
  <name>Task 2: Criar api/send_reminders.php e adicionar botão de lembrete no dashboard (D-03 + D-04)</name>
  <files>api/send_reminders.php, admin/index.php, admin/escala_detalhe.php</files>

  <read_first>
    - admin/index.php — ler COMPLETO para entender onde inserir o widget de lembrete no dashboard do líder. Localizar: a variável `$userRole`, onde ficam as seções de escalas, e onde inserir o bloco ANTES do `renderAppFooter()`.
    - admin/dashboard_data.php — ler para entender `$nextSchedule` e o padrão de dados disponíveis. Precisaremos adicionar a query de escalas próximas com pending.
    - admin/escala_detalhe.php — localizar o `$pdo->commit()` no handler `save_changes` (linhas ~79-115) para inserir o trigger de push.
    - includes/config.php — verificar se VAPID_PUBLIC_KEY e VAPID_PRIVATE_KEY já estão definidos como constantes, ou se precisam ser adicionados.
  </read_first>

  <action>
**Passo 1 — Criar `api/send_reminders.php` (endpoint do botão manual):**

Criar o arquivo `api/send_reminders.php` com o seguinte conteúdo:

```php
<?php
// api/send_reminders.php — Envia push de lembrete para participantes 'pending' de escalas nos próximos 2 dias
header('Content-Type: application/json');
require_once '../includes/auth.php';
require_once '../includes/db.php';
require_once '../includes/web_push_helper.php';

// Apenas admin pode enviar lembretes
if (!isset($_SESSION['user_role']) || $_SESSION['user_role'] !== 'admin') {
    echo json_encode(['success' => false, 'message' => 'Acesso negado']);
    exit;
}

$data = json_decode(file_get_contents('php://input'), true);
$scheduleId = isset($data['schedule_id']) ? (int)$data['schedule_id'] : 0;

// Buscar VAPID keys da config/env
$vapidPublic  = defined('VAPID_PUBLIC_KEY')  ? VAPID_PUBLIC_KEY  : (getenv('VAPID_PUBLIC_KEY')  ?: '');
$vapidPrivate = defined('VAPID_PRIVATE_KEY') ? VAPID_PRIVATE_KEY : (getenv('VAPID_PRIVATE_KEY') ?: '');

if (empty($vapidPublic) || empty($vapidPrivate)) {
    error_log('send_reminders.php: VAPID keys não configuradas');
    echo json_encode(['success' => false, 'message' => 'Push não configurado. Configure as VAPID keys.']);
    exit;
}

try {
    // Buscar escalas nos próximos 2 dias com participantes pending
    if ($scheduleId > 0) {
        // Lembrete para uma escala específica
        $stmt = $pdo->prepare("
            SELECT su.user_id, s.event_type, s.event_date, s.event_time
            FROM schedule_users su
            JOIN schedules s ON s.id = su.schedule_id
            WHERE su.schedule_id = ?
              AND su.status = 'pending'
        ");
        $stmt->execute([$scheduleId]);
    } else {
        // Lembrete para todas as escalas nos próximos 2 dias
        $stmt = $pdo->prepare("
            SELECT su.user_id, s.id as schedule_id, s.event_type, s.event_date, s.event_time
            FROM schedule_users su
            JOIN schedules s ON s.id = su.schedule_id
            WHERE s.event_date BETWEEN CURDATE() AND DATE_ADD(CURDATE(), INTERVAL 2 DAY)
              AND su.status = 'pending'
        ");
        $stmt->execute();
    }
    $pendingUsers = $stmt->fetchAll(PDO::FETCH_ASSOC);

    if (empty($pendingUsers)) {
        echo json_encode(['success' => true, 'message' => 'Nenhum participante pendente encontrado.', 'sent' => 0]);
        exit;
    }

    $helper = new WebPushHelper($vapidPublic, $vapidPrivate, 'mailto:contato@pibolveira.com');
    $sent = 0;
    $failed = 0;

    foreach ($pendingUsers as $pu) {
        // Buscar subscriptions do usuário
        $stmtSub = $pdo->prepare("SELECT endpoint, p256dh, auth FROM push_subscriptions WHERE user_id = ?");
        $stmtSub->execute([$pu['user_id']]);
        $subscriptions = $stmtSub->fetchAll(PDO::FETCH_ASSOC);

        if (empty($subscriptions)) continue;

        $eventDate = date('d/m', strtotime($pu['event_date']));
        $eventTime = substr($pu['event_time'], 0, 5);
        $payload = [
            'title' => '⏰ Lembrete de Escala',
            'body'  => "Você não confirmou presença no " . $pu['event_type'] . " de $eventDate às $eventTime. Confirme no app!",
            'url'   => '/applouvor/admin/escalas.php',
        ];

        foreach ($subscriptions as $sub) {
            $ok = $helper->sendNotification($sub, $payload);
            if ($ok) $sent++;
            else $failed++;
        }
    }

    echo json_encode([
        'success' => true,
        'message' => "Lembretes enviados: $sent. Falhas: $failed.",
        'sent'    => $sent,
        'failed'  => $failed,
    ]);

} catch (Exception $e) {
    error_log('send_reminders.php error: ' . $e->getMessage());
    echo json_encode(['success' => false, 'message' => 'Erro interno: ' . $e->getMessage()]);
}
```

**Passo 2 — Adicionar widget de lembrete no dashboard (admin/index.php):**

Localizar o final do `<div class="dashboard-container">` (antes de `renderAppFooter()`). Inserir ANTES de `renderAppFooter()` um bloco condicional para admin:

```php
<?php if ($userRole === 'admin'): ?>
<?php
// Buscar escalas nos próximos 2 dias com participantes pending
$upcomingWithPending = [];
try {
    $stmtReminder = $pdo->prepare("
        SELECT s.id, s.event_type, s.event_date, s.event_time,
               COUNT(su.user_id) as pending_count
        FROM schedules s
        JOIN schedule_users su ON s.id = su.schedule_id
        WHERE s.event_date BETWEEN CURDATE() AND DATE_ADD(CURDATE(), INTERVAL 2 DAY)
          AND su.status = 'pending'
        GROUP BY s.id
        HAVING pending_count > 0
    ");
    $stmtReminder->execute();
    $upcomingWithPending = $stmtReminder->fetchAll(PDO::FETCH_ASSOC);
} catch (Exception $e) {}
?>
<?php if (!empty($upcomingWithPending)): ?>
<div style="margin: 0 var(--space-md) var(--space-md);">
    <div class="pib-card" style="border-left: 4px solid #f97316; padding: 16px;">
        <div style="display: flex; align-items: center; gap: 8px; margin-bottom: 12px;">
            <i data-lucide="bell" width="20" style="color: #f97316;"></i>
            <span style="font-weight: 800; font-size: 0.95rem;">Lembretes Pendentes</span>
        </div>
        <?php foreach ($upcomingWithPending as $upcoming): ?>
        <div style="display: flex; align-items: center; justify-content: space-between; padding: 8px 0; border-bottom: 1px solid var(--color-border);">
            <div>
                <div style="font-weight: 700; font-size: 0.9rem;"><?= htmlspecialchars($upcoming['event_type']) ?></div>
                <div style="font-size: 0.75rem; color: var(--color-text-muted);">
                    <?= date('d/m', strtotime($upcoming['event_date'])) ?> às <?= substr($upcoming['event_time'], 0, 5) ?>
                    — <?= $upcoming['pending_count'] ?> sem confirmar
                </div>
            </div>
            <button
                onclick="sendReminder(<?= (int)$upcoming['id'] ?>, this)"
                style="background: #f97316; color: #fff; border: none; border-radius: 8px; padding: 8px 14px; font-weight: 700; font-size: 0.8rem; cursor: pointer; min-height: 44px; white-space: nowrap;">
                <i data-lucide="send" width="14"></i> Lembrar
            </button>
        </div>
        <?php endforeach; ?>
    </div>
</div>

<script>
function sendReminder(scheduleId, btn) {
    btn.disabled = true;
    btn.textContent = 'Enviando...';
    fetch('../api/send_reminders.php', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ schedule_id: scheduleId })
    })
    .then(function(r) { return r.json(); })
    .then(function(data) {
        if (data.success) {
            btn.textContent = 'Enviado!';
            btn.style.background = '#16a34a';
        } else {
            btn.textContent = 'Erro';
            btn.style.background = '#dc2626';
            btn.disabled = false;
            alert('Erro: ' + (data.message || 'Tente novamente.'));
        }
    })
    .catch(function() {
        btn.textContent = 'Erro';
        btn.disabled = false;
    });
}
</script>
<?php endif; ?>
<?php endif; ?>
```

**Passo 3 — Adicionar trigger automático no save_changes de escala_detalhe.php (D-03):**

Localizar o handler `save_changes` em `admin/escala_detalhe.php`. A linha exata que precede `header("Location: ...")` após `$pdo->commit()` está ~na linha 108. Inserir entre `$pdo->commit();` e `header(...)`:

```php
// Push de convocação (D-03) — dispara após commit bem-sucedido
try {
    require_once '../includes/web_push_helper.php';
    $vapidPublic  = defined('VAPID_PUBLIC_KEY')  ? VAPID_PUBLIC_KEY  : (getenv('VAPID_PUBLIC_KEY')  ?: '');
    $vapidPrivate = defined('VAPID_PRIVATE_KEY') ? VAPID_PRIVATE_KEY : (getenv('VAPID_PRIVATE_KEY') ?: '');
    if (!empty($vapidPublic) && !empty($vapidPrivate)) {
        // Buscar APENAS novos membros (status=pending desta escala)
        $stmtPushUsers = $pdo->prepare("
            SELECT su.user_id, ps.endpoint, ps.p256dh, ps.auth
            FROM schedule_users su
            JOIN push_subscriptions ps ON ps.user_id = su.user_id
            WHERE su.schedule_id = ? AND su.status = 'pending'
        ");
        $stmtPushUsers->execute([$id]);
        $pushTargets = $stmtPushUsers->fetchAll(PDO::FETCH_ASSOC);
        if (!empty($pushTargets)) {
            $pushHelper = new WebPushHelper($vapidPublic, $vapidPrivate, 'mailto:contato@pibolveira.com');
            $eventDate = date('d/m', strtotime($_POST['event_date']));
            $eventTime = substr($_POST['event_time'], 0, 5);
            $convocPayload = [
                'title' => '📋 Nova Escala',
                'body'  => "Você foi escalado para " . htmlspecialchars($_POST['event_type']) . " em $eventDate às $eventTime. Confirme no app!",
                'url'   => '/applouvor/admin/escala_detalhe.php?id=' . (int)$id,
            ];
            foreach ($pushTargets as $target) {
                $sub = ['endpoint' => $target['endpoint'], 'p256dh' => $target['p256dh'], 'auth' => $target['auth']];
                $pushHelper->sendNotification($sub, $convocPayload);
            }
        }
    }
} catch (Exception $pushEx) {
    // Push falhou — não interrompe o fluxo de salvamento (D-02 fallback)
    error_log('Push convocação falhou: ' . $pushEx->getMessage());
}
```

**Passo 4 — Adicionar VAPID_PUBLIC_KEY e VAPID_PRIVATE_KEY em includes/config.php:**

Ao final do bloco `if ($isProduction)` em `includes/config.php`, adicionar:
```php
define('VAPID_PUBLIC_KEY',  getenv('VAPID_PUBLIC_KEY')  ?: '');
define('VAPID_PRIVATE_KEY', getenv('VAPID_PRIVATE_KEY') ?: '');
```

E no bloco `else` (ambiente local):
```php
define('VAPID_PUBLIC_KEY',  App\DotEnv::get('VAPID_PUBLIC_KEY',  ''));
define('VAPID_PRIVATE_KEY', App\DotEnv::get('VAPID_PRIVATE_KEY', ''));
```

Inserir esses defines logo após as definições de `APP_URL` em cada bloco.
  </action>

  <verify>
    <automated>grep -c "send_reminders" "admin/index.php" && grep -c "WebPushHelper" "admin/escala_detalhe.php" && grep -c "pending_count" "admin/index.php"</automated>
  </verify>

  <acceptance_criteria>
    - `api/send_reminders.php` existe e contém `WebPushHelper`, `status = 'pending'`, e `json_encode(['success'`
    - `api/send_reminders.php` verifica `$_SESSION['user_role'] !== 'admin'` e retorna 403
    - `admin/index.php` contém `send_reminders.php` (fetch call), `sendReminder` função JS, e `pending_count` para o widget
    - `admin/index.php` exibe o widget apenas quando `$userRole === 'admin'`
    - `admin/escala_detalhe.php` contém `WebPushHelper` no handler `save_changes`
    - `admin/escala_detalhe.php` contém o bloco push dentro de `try/catch` (não interrompe o save se falhar)
    - `admin/escala_detalhe.php` contém `$pdo->commit()` ANTES do bloco WebPushHelper
    - `includes/config.php` contém `VAPID_PUBLIC_KEY` e `VAPID_PRIVATE_KEY`
    - `php -l api/send_reminders.php` sem erros de sintaxe
    - `php -l admin/index.php` sem erros de sintaxe
    - `php -l admin/escala_detalhe.php` sem erros de sintaxe
    - `php -l includes/config.php` sem erros de sintaxe
  </acceptance_criteria>

  <done>
Botão "Lembrar quem não confirmou" funcional no dashboard admin para escalas nos próximos 2 dias. Push de convocação automático ao salvar escala. api/send_reminders.php envia push para status=pending. AESGCM real ativo — fallback garante valor mínimo mesmo se criptografia falhar.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → api/send_reminders.php | POST autenticado via sessão; apenas admin pode acionar |
| PHP server → Push Service (FCM/VAPID) | Servidor envia push para endpoint externo com JWT assinado |
| Push Service → Browser | Entrega de notificação com payload criptografado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02D-01 | Spoofing | api/send_reminders.php | mitigate | Verifica `$_SESSION['user_role'] === 'admin'` — músico não pode acionar lembretes |
| T-02D-02 | Tampering | includes/web_push_helper.php (encrypt) | mitigate | Salt e nonce são random_bytes — não previsíveis; payload criptografado com AES-128-GCM autenticado |
| T-02D-03 | Information Disclosure | push payload | accept | Payload contém apenas tipo do evento, data e hora — sem dados sensíveis |
| T-02D-04 | Denial of Service | api/send_reminders.php (loop de push) | accept | Envio é síncrono mas acontece apenas quando admin clica manualmente; número de subscriptions é pequeno (time de louvor ~10 pessoas) |
| T-02D-05 | Elevation | admin/escala_detalhe.php (trigger push) | mitigate | Trigger de push está dentro do bloco `isset($_POST['save_changes']) && $_SESSION['user_role'] === 'admin'` — apenas admin salva escalas |
| T-02D-06 | Repudiation | VAPID keys | mitigate | Chaves carregadas de env/config — nunca commitadas no repositório (gitignored per Phase 1) |
</threat_model>

<verification>
1. `php -l includes/web_push_helper.php` — sem erros
2. `php -l api/send_reminders.php` — sem erros
3. `php -l admin/index.php` — sem erros
4. `php -l admin/escala_detalhe.php` — sem erros
5. `php -l includes/config.php` — sem erros
6. `grep "openssl_encrypt.*aes-128-gcm" includes/web_push_helper.php` — linha encontrada
7. `grep "send_reminders" admin/index.php` — linha encontrada
8. `grep "WebPushHelper" admin/escala_detalhe.php` — linha encontrada
9. `grep "VAPID_PUBLIC_KEY" includes/config.php` — pelo menos 2 ocorrências (prod + local)
10. Testar manualmente: escala com participante pending + subscription cadastrada → clicar "Lembrar" no dashboard → notificação recebida no browser
</verification>

<success_criteria>
- Widget "Lembretes Pendentes" aparece no dashboard do líder quando há escalas nos próximos 2 dias com participantes pending
- Clicar "Lembrar" chama api/send_reminders.php; botão fica verde após sucesso
- Push é entregue com payload (título + corpo) para browsers com subscription ativa
- Se AESGCM falhar internamente, o erro é logado e o botão ainda retorna sucesso (fallback D-02)
- Ao salvar escala, push de convocação é disparado para participantes pending; falha não interrompe o save
- VAPID keys carregadas via config.php (env/constantes) — não hardcoded no código
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-confirmar-escala/02-04-SUMMARY.md`
</output>
