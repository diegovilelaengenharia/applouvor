---
phase: 02-confirmar-escala
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - admin/escala_detalhe.php
  - assets/css/pages/detail_v3.css
autonomous: true
requirements:
  - ESC-01
  - ESC-02

must_haves:
  truths:
    - "Músico que é participante da escala vê footer sticky com botões 'Confirmar' e 'Recusar' quando seu status é 'pending'"
    - "Clicar em 'Confirmar' ou 'Recusar' chama a API via fetch sem reload da página"
    - "Após AJAX success, footer transita para estado de status confirmado/recusado com botão 'Alterar'"
    - "Clicar em 'Alterar' reexibe os botões originais de confirmação"
    - "Footer está fixo na parte inferior da tela com safe-area-inset para iOS"
  artifacts:
    - path: "admin/escala_detalhe.php"
      provides: "Footer sticky de confirmação com JS AJAX"
      contains: "id=\"confirm-footer\""
    - path: "assets/css/pages/detail_v3.css"
      provides: "CSS do footer sticky"
      contains: ".confirm-footer"
  key_links:
    - from: "admin/escala_detalhe.php (footer buttons)"
      to: "api/confirm_scale.php"
      via: "fetch('../api/confirm_scale.php')"
      pattern: "fetch\\(.*api/confirm_scale\\.php"
---

<objective>
Adicionar footer sticky com botões "Confirmar" e "Recusar" na view do músico em `admin/escala_detalhe.php`. O músico clica, o status é atualizado via AJAX sem reload, e o footer transita para exibir o status atual com opção de alterar.

Purpose: Entregar ESC-01 (botões visíveis) e ESC-02 (confirmação via AJAX com feedback imediato). Esta é a feature central da Phase 2 — sem ela nada mais tem valor.
Output: Footer sticky funcional com AJAX, dois estados (action/status), CSS no design system existente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-confirmar-escala/02-CONTEXT.md

<interfaces>
<!-- Contratos existentes que o executor deve usar diretamente. -->

De api/confirm_scale.php:
- Aceita: POST JSON body `{"schedule_id": int, "status": "confirmed"|"declined"}`
- Retorna: `{"success": true|false, "message": "..."}`
- Header Content-Type: application/json (requerido)

De admin/escala_detalhe.php (variáveis PHP disponíveis na view):
- `$myMemberData` — array|null. Se null, usuário não é participante. Se array:
  - `$myMemberData['status']` — string: 'pending', 'confirmed', 'declined', 'absent'
  - `$myMemberData['user_id']` — int
- `$id` — int, schedule_id da escala atual (vem de $_GET['id'])
- `$isEditable` — bool, true quando admin está no modo edição

De assets/css/core/variables.css (padrão do design system):
- `--primary`: #3B82F6 (azul — botão confirmar)
- `--bg-surface`: fundo dos cards
- `--border-subtle`: bordas
- `.btn-primary`: azul principal
- `.btn-danger`: vermelho (recusar)
- `.pib-card`: componente base com 12px radius
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar CSS do footer sticky e estados de status em detail_v3.css</name>
  <files>assets/css/pages/detail_v3.css</files>

  <read_first>
    - assets/css/pages/detail_v3.css — ler o arquivo inteiro para entender estrutura existente e adicionar ao final sem conflitos
    - assets/css/core/variables.css — verificar variáveis de cor disponíveis (--primary, --danger, --green-*, etc.)
  </read_first>

  <action>
Adicionar ao FINAL de `assets/css/pages/detail_v3.css` os seguintes blocos CSS (não modificar CSS existente):

```css
/* === CONFIRM FOOTER STICKY (Phase 2A) === */
.confirm-footer {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    background: var(--bg-surface);
    border-top: 1px solid var(--border-subtle);
    padding: 12px 20px;
    padding-bottom: calc(12px + env(safe-area-inset-bottom, 16px));
    display: flex;
    gap: 12px;
    z-index: 100;
    box-shadow: 0 -4px 16px rgba(0,0,0,0.08);
}

.confirm-footer .btn-confirm {
    flex: 1;
    padding: 14px;
    background: #3B82F6;
    color: #fff;
    border: none;
    border-radius: 12px;
    font-size: 1rem;
    font-weight: 700;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    min-height: 44px;
    transition: opacity 0.2s ease;
}

.confirm-footer .btn-confirm:active {
    opacity: 0.85;
}

.confirm-footer .btn-decline {
    flex: 1;
    padding: 14px;
    background: transparent;
    color: #ef4444;
    border: 2px solid #ef4444;
    border-radius: 12px;
    font-size: 1rem;
    font-weight: 700;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    min-height: 44px;
    transition: all 0.2s ease;
}

.confirm-footer .btn-decline:active {
    background: #fef2f2;
}

/* Estado pós-confirmação: exibe status atual */
.confirm-footer-status {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    background: var(--bg-surface);
    border-top: 1px solid var(--border-subtle);
    padding: 12px 20px;
    padding-bottom: calc(12px + env(safe-area-inset-bottom, 16px));
    display: flex;
    align-items: center;
    justify-content: space-between;
    z-index: 100;
    box-shadow: 0 -4px 16px rgba(0,0,0,0.08);
}

.confirm-footer-status .status-label {
    font-size: 1rem;
    font-weight: 700;
    display: flex;
    align-items: center;
    gap: 8px;
}

.confirm-footer-status .status-label.is-confirmed {
    color: #16a34a;
}

.confirm-footer-status .status-label.is-declined {
    color: #dc2626;
}

.confirm-footer-status .btn-change {
    background: transparent;
    border: 1px solid var(--border-subtle);
    color: var(--text-muted);
    padding: 8px 16px;
    border-radius: 8px;
    font-size: 0.85rem;
    cursor: pointer;
    min-height: 44px;
    font-weight: 600;
}

/* Espaço extra no final da página para não esconder conteúdo atrás do footer */
.has-confirm-footer {
    padding-bottom: 90px;
}
```
  </action>

  <verify>
    <automated>grep -c "confirm-footer" "assets/css/pages/detail_v3.css"</automated>
  </verify>

  <acceptance_criteria>
    - `assets/css/pages/detail_v3.css` contém a classe `.confirm-footer` com `position: fixed`
    - Contém `padding-bottom: calc(12px + env(safe-area-inset-bottom, 16px))`
    - Contém `.confirm-footer-status` com `.status-label.is-confirmed` e `.status-label.is-declined`
    - Contém `.btn-confirm` com `min-height: 44px` (toque mínimo)
    - Contém `.btn-decline` com `min-height: 44px`
    - Contém `.has-confirm-footer` com `padding-bottom: 90px`
    - CSS existente não foi modificado (as linhas anteriores ao bloco adicionado estão intactas)
  </acceptance_criteria>

  <done>CSS do footer sticky adicionado ao final de detail_v3.css sem modificar estilos existentes.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar footer sticky com AJAX e transição de estado em escala_detalhe.php</name>
  <files>admin/escala_detalhe.php</files>

  <read_first>
    - admin/escala_detalhe.php — ler COMPLETO para entender onde inserir o footer e o JS (localizar: $myMemberData, $isEditable, renderAppFooter(), a section VIEW MODE CONTENT)
    - api/confirm_scale.php — verificar interface da API (já lido acima — POST JSON, retorna {success, message})
  </read_first>

  <action>
**Parte 1 — Adicionar classe `has-confirm-footer` no wrapper quando footer será exibido:**

Na linha que abre `<div class="scale-detail-wrapper">` (ao redor da linha 194), condicionalmente adicionar a classe extra quando `$myMemberData !== null` e `$myMemberData['status'] === 'pending'`:

```php
<div class="scale-detail-wrapper<?= ($myMemberData && $myMemberData['status'] === 'pending') ? ' has-confirm-footer' : '' ?>">
```

**Parte 2 — Adicionar footer sticky ANTES de `<?php renderAppFooter(); ?>`:**

Localizar a última linha do arquivo com `renderAppFooter()` (linha ~803). Imediatamente ANTES dela, inserir o bloco PHP + HTML do footer:

```php
<?php if ($myMemberData !== null && !$isEditable):
    $currentStatus = $myMemberData['status'];
    $scheduleIdForJs = (int)$id;
?>

<?php if ($currentStatus === 'pending'): ?>
<div class="confirm-footer" id="confirm-footer">
    <button class="btn-confirm" onclick="confirmPresence('confirmed')">
        <i data-lucide="check-circle" width="20"></i> Confirmar
    </button>
    <button class="btn-decline" onclick="confirmPresence('declined')">
        <i data-lucide="x-circle" width="20"></i> Recusar
    </button>
</div>
<?php elseif ($currentStatus === 'confirmed'): ?>
<div class="confirm-footer-status" id="confirm-footer">
    <span class="status-label is-confirmed">
        <i data-lucide="check-circle" width="20"></i> Confirmado
    </span>
    <button class="btn-change" onclick="showConfirmButtons()">Alterar</button>
</div>
<?php elseif ($currentStatus === 'declined'): ?>
<div class="confirm-footer-status" id="confirm-footer">
    <span class="status-label is-declined">
        <i data-lucide="x-circle" width="20"></i> Recusado
    </span>
    <button class="btn-change" onclick="showConfirmButtons()">Alterar</button>
</div>
<?php endif; ?>

<script>
(function() {
    var scheduleId = <?= $scheduleIdForJs ?>;

    function confirmPresence(status) {
        var footer = document.getElementById('confirm-footer');
        var btnConfirm = footer.querySelector('.btn-confirm');
        var btnDecline = footer.querySelector('.btn-decline');
        if (btnConfirm) btnConfirm.disabled = true;
        if (btnDecline) btnDecline.disabled = true;

        fetch('../api/confirm_scale.php', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ schedule_id: scheduleId, status: status })
        })
        .then(function(r) { return r.json(); })
        .then(function(data) {
            if (data.success) {
                var isConfirmed = (status === 'confirmed');
                var labelText = isConfirmed ? 'Confirmado' : 'Recusado';
                var labelClass = isConfirmed ? 'is-confirmed' : 'is-declined';
                var iconName = isConfirmed ? 'check-circle' : 'x-circle';

                footer.outerHTML = '<div class="confirm-footer-status" id="confirm-footer">'
                    + '<span class="status-label ' + labelClass + '">'
                    + '<i data-lucide="' + iconName + '" width="20"></i> ' + labelText
                    + '</span>'
                    + '<button class="btn-change" onclick="showConfirmButtons()">Alterar</button>'
                    + '</div>';

                // Re-render Lucide icons no novo elemento
                if (typeof lucide !== 'undefined') lucide.createIcons();

                // Atualizar padding do wrapper
                var wrapper = document.querySelector('.scale-detail-wrapper');
                if (wrapper) wrapper.classList.remove('has-confirm-footer');
            } else {
                alert('Erro ao salvar: ' + (data.message || 'Tente novamente.'));
                if (btnConfirm) btnConfirm.disabled = false;
                if (btnDecline) btnDecline.disabled = false;
            }
        })
        .catch(function(err) {
            alert('Erro de conexão. Tente novamente.');
            if (btnConfirm) btnConfirm.disabled = false;
            if (btnDecline) btnDecline.disabled = false;
        });
    }

    function showConfirmButtons() {
        var footer = document.getElementById('confirm-footer');
        footer.outerHTML = '<div class="confirm-footer" id="confirm-footer">'
            + '<button class="btn-confirm" onclick="confirmPresence(\'confirmed\')">'
            + '<i data-lucide="check-circle" width="20"></i> Confirmar'
            + '</button>'
            + '<button class="btn-decline" onclick="confirmPresence(\'declined\')">'
            + '<i data-lucide="x-circle" width="20"></i> Recusar'
            + '</button>'
            + '</div>';

        if (typeof lucide !== 'undefined') lucide.createIcons();

        var wrapper = document.querySelector('.scale-detail-wrapper');
        if (wrapper) wrapper.classList.add('has-confirm-footer');
    }

    // Expor para onclick inline
    window.confirmPresence = confirmPresence;
    window.showConfirmButtons = showConfirmButtons;
})();
</script>

<?php endif; ?>
```

**Parte 3 — Verificar que o $isEditable não exibe footer quando em modo edição:**
O bloco `if ($myMemberData !== null && !$isEditable)` já garante isso.
  </action>

  <verify>
    <automated>grep -c "confirm-footer" "admin/escala_detalhe.php"</automated>
  </verify>

  <acceptance_criteria>
    - `admin/escala_detalhe.php` contém `id="confirm-footer"` (pelo menos 1 ocorrência)
    - Contém `fetch('../api/confirm_scale.php'`
    - Contém `schedule_id: scheduleId`
    - Contém `status: status` no body do fetch
    - Contém `$myMemberData !== null && !$isEditable` como condição de exibição do footer
    - Contém `$myMemberData['status'] === 'pending'` para a classe `has-confirm-footer`
    - Contém `window.confirmPresence` e `window.showConfirmButtons`
    - Bloco PHP do footer está posicionado ANTES de `renderAppFooter()`
    - Código PHP válido (sem erros de sintaxe verificáveis com `php -l admin/escala_detalhe.php`)
  </acceptance_criteria>

  <done>
Footer sticky exibido para músico participante com status pending. Clicar confirma/recusa via AJAX. Footer transita para estado de status com botão "Alterar". Funciona em iOS/Android (safe-area).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → api/confirm_scale.php | Usuário autenticado envia status via AJAX; API valida sessão e valores |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02A-01 | Spoofing | api/confirm_scale.php | mitigate | API já verifica `$_SESSION['user_id']`; retorna error se userId=0 |
| T-02A-02 | Tampering | api/confirm_scale.php | mitigate | `in_array($status, ['confirmed', 'declined'])` já existe na API — valores fora da lista são rejeitados |
| T-02A-03 | Elevation | admin/escala_detalhe.php | mitigate | Footer só renderizado quando `$myMemberData !== null` (usuário é participante) — líder sem vínculo não vê o footer |
</threat_model>

<verification>
1. `php -l admin/escala_detalhe.php` — sem erros de sintaxe PHP
2. `grep -c "confirm-footer" admin/escala_detalhe.php` — resultado >= 2
3. `grep "fetch.*api/confirm_scale" admin/escala_detalhe.php` — linha encontrada
4. `grep "safe-area-inset-bottom" assets/css/pages/detail_v3.css` — linha encontrada
5. Testar manualmente: acessar escala onde usuário é participante com status pending → footer visível. Clicar "Confirmar" → footer transita. Clicar "Alterar" → botões reaparecem.
</verification>

<success_criteria>
- Músico participante vê footer sticky com botões "Confirmar" / "Recusar" quando status = pending
- Botões têm altura mínima 44px (padrão toque móvel)
- Clicar "Confirmar" ou "Recusar" chama a API sem reload; footer mostra status confirmado/recusado
- Clicar "Alterar" reexibe os botões
- Footer não aparece para não-participantes, nem em modo edição do admin
- Em iOS Safari o footer não se sobrepõe à home bar (safe-area-inset-bottom aplicado)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-confirmar-escala/02-01-SUMMARY.md`
</output>
