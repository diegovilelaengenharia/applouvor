---
phase: 03-roteiro
plan: "04"
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - admin/escala_detalhe.php
  - assets/css/pages/detail_v3.css
autonomous: true
requirements:
  - ROT-05

must_haves:
  truths:
    - "Cards de música na seção Repertório mostram tom customizado quando definido no roteiro"
    - "Quando custom_tone está definido no roteiro para uma música, o card mostra 'Tom: D (Roteiro)' — não o tom padrão"
    - "Quando custom_tone não está definido, o card exibe o tom padrão da songs.tone sem qualquer indicador"
    - "A lógica de resolução de tom é server-side (PHP), não client-side"
    - "UI do modal de adição (Plan 03-02) mostra tom padrão da música como placeholder no campo custom_tone"
  artifacts:
    - path: "admin/escala_detalhe.php"
      provides: "Cards de repertório com custom_tone integrado do roteiro"
      contains: "custom_tone.*roteiro\|roteiro.*custom_tone"
    - path: "assets/css/pages/detail_v3.css"
      provides: "Badge de tom customizado diferenciado"
      contains: ".song-tone-custom"
  key_links:
    - from: "admin/escala_detalhe.php (song cards)"
      to: "schedule_roteiro.custom_tone"
      via: "PHP array lookup em $roteiro"
      pattern: "custom_tone"
---

<objective>
Integrar o tom customizado do roteiro nos cards de música da seção Repertório. Quando uma música tem `custom_tone` definido no roteiro, os cards mostram esse tom com badge diferenciado. Também melhorar o UX do modal de adição (placeholder com tom padrão).

Purpose: Fechar ROT-05 completamente — o tom customizado não é só salvo (Plan 3A) e exibido no roteiro (Plan 3C), mas também aparece nos cards de repertório que o músico já conhece.
Output: Cards de música em escala_detalhe.php exibem custom_tone quando relevante + badge visual diferenciado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-roteiro/03-CONTEXT.md

<interfaces>
De admin/escala_detalhe.php (estado após Plans 03-02 e 03-03):
- `$roteiro` — array de itens do roteiro, já carregado (adicionado em 03-03)
  - Cada item: {id, item_type, song_id, custom_tone, song_title, song_tone, ...}
- `$songs` — array de músicas do repertório: [{song_id, title, artist, tone, ...}]
- Seção de Repertório renderiza cards com `$song['tone']` — AQUI adicionar override de custom_tone

Lógica de resolução:
1. Construir map: `$customToneMap[song_id] = custom_tone` a partir de $roteiro onde item_type='musica' e custom_tone != null
2. Na renderização de cada song card, verificar se `$customToneMap[$song['song_id']]` existe
3. Se existe → exibir custom_tone + badge ".song-tone-custom"
4. Se não → exibir tone padrão sem badge especial

Modal de adição (JS — Plan 03-02):
- O select `#roteiro-song` tem `data-tone` em cada option
- Ao selecionar uma música, preencher `#roteiro-custom-tone` como placeholder (não como valor)
  → Adicionar `onchange` no select para auto-preencher placeholder com o tom padrão da música
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar badge CSS para tom customizado em detail_v3.css</name>
  <files>assets/css/pages/detail_v3.css</files>

  <read_first>
    - assets/css/pages/detail_v3.css — ler o final do arquivo (após estilos adicionados em 03-02 e 03-03) para adicionar ao final sem conflito
  </read_first>

  <action>
Adicionar ao FINAL de `assets/css/pages/detail_v3.css`:

```css
/* === TOM CUSTOMIZADO — BADGE (Phase 3D) === */
.song-tone-badge {
    display: inline-flex;
    align-items: center;
    gap: 3px;
    font-size: 0.72rem;
    font-weight: 700;
    padding: 2px 7px;
    border-radius: 20px;
    letter-spacing: 0.02em;
    vertical-align: middle;
}

.song-tone-default {
    background: var(--bg-body);
    color: var(--text-muted);
    border: 1px solid var(--border-subtle);
}

.song-tone-custom {
    background: rgba(249,115,22,0.12);
    color: #c2410c;
    border: 1px solid rgba(249,115,22,0.3);
}

.song-tone-custom::before {
    content: "✦ ";
    font-size: 0.65rem;
    opacity: 0.8;
}
```
  </action>

  <verify>
    <automated>grep -c "song-tone-custom" "assets/css/pages/detail_v3.css"</automated>
  </verify>

  <acceptance_criteria>
    - Arquivo contém `.song-tone-custom` com background laranja (rgba(249,115,22,...))
    - Contém `.song-tone-default` para tom padrão
    - Contém `.song-tone-badge` como base
    - CSS existente não foi modificado
  </acceptance_criteria>

  <done>Badge de tom customizado adicionado ao detail_v3.css com diferenciação visual laranja.</done>
</task>

<task type="auto">
  <name>Task 2: Integrar custom_tone nos cards de repertório em escala_detalhe.php</name>
  <files>admin/escala_detalhe.php</files>

  <read_first>
    - admin/escala_detalhe.php — localizar:
      1. A seção "Buscar Dados" onde $roteiro já foi adicionado — para adicionar o $customToneMap logo após
      2. A renderização dos song cards na seção Repertório (modo view) — onde `$song['tone']` é exibido
      3. O select `#roteiro-song` no modal de adição — para adicionar auto-preenchimento do placeholder
  </read_first>

  <action>
**Parte 1 — Construir $customToneMap após carregar $roteiro (na seção BUSCAR DADOS):**

Logo após a linha `$roteiro = $stmtRoteiro->fetchAll(...)`, adicionar:

```php
// Mapa de custom_tone por song_id (para override nos cards de repertório — ROT-05)
$customToneMap = [];
foreach ($roteiro as $rItem) {
    if ($rItem['item_type'] === 'musica' && $rItem['song_id'] && !empty($rItem['custom_tone'])) {
        $customToneMap[(int)$rItem['song_id']] = $rItem['custom_tone'];
    }
}
```

**Parte 2 — Modificar renderização do tom nos song cards (modo view):**

Localizar na seção de Repertório (modo view) onde o tom da música é exibido. Atualmente há algo como:
```php
<div class="song-artist"><?= htmlspecialchars($song['artist']) ?> • <?= htmlspecialchars($song['tone']) ?></div>
```

Substituir a exibição do tom para usar custom_tone quando disponível:

```php
<?php
$resolvedTone = $customToneMap[$song['song_id']] ?? null;
$isCustomTone = !empty($resolvedTone);
$toneDisplay  = $isCustomTone ? $resolvedTone : ($song['tone'] ?? '');
?>
<div class="song-artist">
    <?= htmlspecialchars($song['artist']) ?>
    <?php if ($toneDisplay): ?>
        <span class="song-tone-badge <?= $isCustomTone ? 'song-tone-custom' : 'song-tone-default' ?>">
            <?= htmlspecialchars($toneDisplay) ?>
        </span>
    <?php endif; ?>
</div>
```

**Parte 3 — Auto-preencher placeholder do campo custom_tone no modal (JS):**

Localizar o script de roteiro adicionado em Plan 03-02. No `select#roteiro-song`, adicionar `onchange` para auto-preencher o placeholder:

Modificar a linha do select `#roteiro-song` no modal para:
```html
<select id="roteiro-song" class="form-input w-full" onchange="onRoteiroSongChange(this)">
```

E adicionar a função `onRoteiroSongChange` ao bloco de script do roteiro:
```javascript
window.onRoteiroSongChange = function(sel) {
    var tone = sel.options[sel.selectedIndex].getAttribute('data-tone') || '';
    var toneInput = document.getElementById('roteiro-custom-tone');
    if (toneInput) toneInput.placeholder = tone ? 'Ex: ' + tone + ' (padrão — deixe vazio para manter)' : 'Ex: D, Em, F#m...';
};
```
  </action>

  <verify>
    <automated>php -l admin/escala_detalhe.php</automated>
  </verify>

  <acceptance_criteria>
    - `php -l admin/escala_detalhe.php` — sem erros de sintaxe PHP
    - Contém `$customToneMap = [];` com loop em `$roteiro`
    - Contém `$resolvedTone = $customToneMap[$song['song_id']] ?? null`
    - Contém `song-tone-custom` como classe condicional no badge do tom
    - Contém `htmlspecialchars($toneDisplay)` — sanitização do tom antes de exibir
    - Contém `onRoteiroSongChange` como função JS
    - Select `#roteiro-song` tem `onchange="onRoteiroSongChange(this)"`
    - Lógica de resolução é server-side em PHP (não apenas JS)
  </acceptance_criteria>

  <done>
custom_tone integrado nos cards de repertório via $customToneMap. Badge laranja para tom customizado, badge cinza para tom padrão. Placeholder do modal auto-preenche com tom padrão da música selecionada.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PHP render → HTML | custom_tone vem do banco via $roteiro — server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03D-01 | XSS | $toneDisplay no HTML | mitigate | `htmlspecialchars($toneDisplay)` antes de inserir no HTML |
| T-03D-02 | Logic | $customToneMap song_id int cast | mitigate | `(int)$rItem['song_id']` no map e `$song['song_id']` na lookup — types consistentes |
</threat_model>

<verification>
1. `php -l admin/escala_detalhe.php` — sem erros de sintaxe PHP
2. `grep "customToneMap" admin/escala_detalhe.php` — pelo menos 2 ocorrências (definição + lookup)
3. `grep "song-tone-custom" admin/escala_detalhe.php` — encontrado na renderização
4. `grep "onRoteiroSongChange" admin/escala_detalhe.php` — encontrado no select e na função JS
5. Testar: criar item no roteiro tipo "musica" com custom_tone="Em" → no card de repertório a música mostra badge laranja "Em"
6. Testar: música sem custom_tone no roteiro → card mostra tom padrão sem badge laranja
7. Testar: no modal, selecionar música → placeholder do campo tom auto-preenche com "Ex: D (padrão)"
8. Testar: música não presente no roteiro → card mostra tom padrão (nenhum badge laranja)
</verification>

<success_criteria>
- Cards de música na seção Repertório exibem custom_tone com badge laranja quando definido no roteiro
- Badge cinza discreto para tom padrão da songs.tone (não poluí visuamente)
- Lógica de resolução server-side: PHP monta $customToneMap antes de renderizar
- No modal de adição, selecionar música auto-preenche o placeholder do campo de tom customizado
- htmlspecialchars aplicado em todos os dados de tom antes de inserir no HTML
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-roteiro/03-04-SUMMARY.md`
</output>
