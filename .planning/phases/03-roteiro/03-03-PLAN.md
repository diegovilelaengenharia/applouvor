---
phase: 03-roteiro
plan: "03"
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - admin/escala_detalhe.php
  - assets/css/pages/detail_v3.css
autonomous: true
requirements:
  - ROT-04
  - ROT-05

must_haves:
  truths:
    - "Seção 'Roteiro de Culto' aparece no modo view (!$isEditable) de escala_detalhe.php"
    - "Seção fica oculta (ou mostra mensagem 'Roteiro não definido') se não houver itens"
    - "Cada item mostra ícone Lucide por tipo + título/nome da música + tipo como label"
    - "Itens de tipo 'musica' mostram o tom: custom_tone se definido, senão song_tone padrão"
    - "nota_interna NÃO aparece na view do músico"
    - "Admin vê nota_interna com destaque visual sutil (ícone olho, cor azul discreto)"
    - "Lista respeita order_position ASC"
    - "Seção tem número de itens no header (ex: 'Roteiro · 6 itens')"
  artifacts:
    - path: "admin/escala_detalhe.php"
      provides: "Seção de roteiro no modo view (read-only)"
      contains: "roteiro-view-section"
    - path: "assets/css/pages/detail_v3.css"
      provides: "CSS da seção de view do roteiro"
      contains: ".roteiro-view-item"
  key_links:
    - from: "admin/escala_detalhe.php (roteiro view section)"
      to: "schedule_roteiro (via PHP query)"
      via: "PDO SELECT"
      pattern: "schedule_roteiro"
---

<objective>
Adicionar a view read-only do Roteiro de Culto na seção de visualização (`!$isEditable`) de `admin/escala_detalhe.php`. O músico vê o roteiro ordenado com ícone por tipo e tom da música. O admin vê a mesma view mais as notas internas.

Purpose: Entregar ROT-04 (músico visualiza roteiro) e ROT-05 (tom customizado visível). O músico acessa o app antes do culto e sabe a ordem de tudo que vai acontecer.
Output: Seção de roteiro no modo view com dados do banco, sem nota_interna para músicos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-roteiro/03-CONTEXT.md

<interfaces>
De admin/escala_detalhe.php:
- Query do roteiro deve ser adicionada na seção "BUSCAR DADOS" (ao redor da linha 154 do arquivo original)
- `$id` — int, schedule_id da escala atual
- `$isEditable` — bool
- `$_SESSION['user_role']` — 'admin' ou 'user'
- Inserir seção de view ENTRE a seção de Repertório e a seção de Comentários, dentro de `else:` (modo view)

Schema de schedule_roteiro (criado em 03-01):
- id, schedule_id, order_position, item_type, title, song_id, custom_tone, nota_interna
- JOIN songs: song_title=songs.title, song_artist=songs.artist, song_tone=songs.tone

Regra ROT-05 — Tom a exibir:
- Se `custom_tone` não é NULL/vazio → exibir `custom_tone`  
- Senão → exibir `song_tone` (tom padrão da música)
- Para itens sem song_id → não exibir tom

Ícones Lucide por tipo (ver 03-CONTEXT.md):
- musica→music, oracao→hands, palavra→book-open, anuncio→megaphone, intervalo→coffee, livre→more-horizontal
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar query do roteiro na seção de busca de dados e CSS da view</name>
  <files>admin/escala_detalhe.php, assets/css/pages/detail_v3.css</files>

  <read_first>
    - admin/escala_detalhe.php — localizar seção "// --- BUSCAR DADOS ---" (em torno da linha 154) onde ficam as queries $schedule, $team, $songs, $comments — adicionar query do roteiro logo após $songs
    - assets/css/pages/detail_v3.css — ler o final do arquivo para adicionar estilos da view-only após os estilos de edit já adicionados no Plan 03-02
  </read_first>

  <action>
**Parte 1 — Adicionar query do roteiro em admin/escala_detalhe.php:**

Localizar após a query `$comments` (linha ~207) e adicionar:

```php
// Buscar Roteiro de Culto
$stmtRoteiro = $pdo->prepare("
    SELECT r.id, r.order_position, r.item_type, r.title,
           r.song_id, r.custom_tone, r.nota_interna,
           s.title as song_title, s.artist as song_artist, s.tone as song_tone
    FROM schedule_roteiro r
    LEFT JOIN songs s ON s.id = r.song_id
    WHERE r.schedule_id = ?
    ORDER BY r.order_position ASC, r.id ASC
");
$stmtRoteiro->execute([$id]);
$roteiro = $stmtRoteiro->fetchAll(PDO::FETCH_ASSOC);
```

**Parte 2 — Adicionar CSS da view do roteiro ao final de detail_v3.css:**

```css
/* === ROTEIRO DE CULTO — VIEW (Phase 3C) === */
.roteiro-view-section {
    margin-bottom: 16px;
}

.roteiro-view-item {
    display: flex;
    align-items: flex-start;
    gap: 12px;
    padding: 12px 0;
    border-bottom: 1px solid var(--border-subtle);
}

.roteiro-view-item:last-child {
    border-bottom: none;
}

.roteiro-view-num {
    font-size: 0.75rem;
    font-weight: 700;
    color: var(--text-muted);
    width: 20px;
    flex-shrink: 0;
    padding-top: 2px;
    text-align: right;
}

.roteiro-view-icon {
    color: var(--primary);
    flex-shrink: 0;
    display: flex;
    align-items: flex-start;
    padding-top: 2px;
}

.roteiro-view-info {
    flex: 1;
    min-width: 0;
}

.roteiro-view-title {
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--text-main);
    line-height: 1.3;
}

.roteiro-view-meta {
    font-size: 0.78rem;
    color: var(--text-muted);
    margin-top: 3px;
    display: flex;
    align-items: center;
    gap: 8px;
    flex-wrap: wrap;
}

.roteiro-view-tone {
    background: var(--primary);
    color: #fff;
    font-size: 0.7rem;
    font-weight: 700;
    padding: 2px 7px;
    border-radius: 20px;
    letter-spacing: 0.03em;
}

.roteiro-view-nota {
    font-size: 0.78rem;
    color: var(--primary);
    margin-top: 5px;
    padding: 6px 10px;
    background: rgba(59,130,246,0.07);
    border-radius: 6px;
    border-left: 3px solid var(--primary);
    display: flex;
    align-items: flex-start;
    gap: 6px;
    font-style: italic;
}

.roteiro-view-empty {
    padding: 16px;
    text-align: center;
    color: var(--text-muted);
    font-size: 0.85rem;
    font-style: italic;
}
```
  </action>

  <verify>
    <automated>php -l admin/escala_detalhe.php</automated>
  </verify>

  <acceptance_criteria>
    - `php -l admin/escala_detalhe.php` — sem erros de sintaxe
    - Arquivo contém `$stmtRoteiro = $pdo->prepare(` com `SELECT` de `schedule_roteiro`
    - Contém `LEFT JOIN songs s ON s.id = r.song_id`
    - Contém `ORDER BY r.order_position ASC`
    - Variável `$roteiro` populada com `fetchAll(PDO::FETCH_ASSOC)`
    - detail_v3.css contém `.roteiro-view-item` com `border-bottom`
    - detail_v3.css contém `.roteiro-view-tone` com `background: var(--primary)` e `border-radius: 20px`
    - detail_v3.css contém `.roteiro-view-nota` com `border-left: 3px solid var(--primary)`
  </acceptance_criteria>

  <done>Query do roteiro adicionada na seção de busca de dados. CSS da view adicionado ao detail_v3.css.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar seção de view do roteiro em escala_detalhe.php (modo view)</name>
  <files>admin/escala_detalhe.php</files>

  <read_first>
    - admin/escala_detalhe.php — localizar a seção VIEW MODE CONTENT, especificamente:
      1. A div `.detail-section` do "Repertório" (contém song-list)
      2. A div `.detail-section` de "Comentários" (id="comments")
      Inserir a seção do roteiro ENTRE Repertório e Comentários
  </read_first>

  <action>
Dentro do bloco `else:` (modo view), APÓS a seção `.detail-section` do Repertório e ANTES da seção `.detail-section` de Comentários, inserir:

```php
        <!-- ROTEIRO DE CULTO (View Mode — Read-Only) -->
        <?php if (!empty($roteiro)): ?>
        <div class="detail-section roteiro-view-section">
            <div class="section-header">
                <span class="section-title">Roteiro de Culto</span>
                <span class="section-count"><?= count($roteiro) ?></span>
            </div>

            <?php
            $roteiroIcons = [
                'musica'    => 'music',
                'oracao'    => 'hands',
                'palavra'   => 'book-open',
                'anuncio'   => 'megaphone',
                'intervalo' => 'coffee',
                'livre'     => 'more-horizontal',
            ];
            $roteiroLabels = [
                'musica'    => 'Música',
                'oracao'    => 'Oração',
                'palavra'   => 'Palavra',
                'anuncio'   => 'Anúncio',
                'intervalo' => 'Intervalo',
                'livre'     => 'Livre',
            ];
            foreach ($roteiro as $idx => $item):
                $icon  = $roteiroIcons[$item['item_type']]  ?? 'more-horizontal';
                $label = $roteiroLabels[$item['item_type']] ?? $item['item_type'];

                // Título a exibir: título customizado ou nome da música
                $displayTitle = ($item['item_type'] === 'musica' && $item['song_title'])
                    ? $item['song_title']
                    : ($item['title'] ?: $label);

                // Tom: custom_tone tem prioridade sobre song_tone (ROT-05)
                $displayTone = null;
                if ($item['item_type'] === 'musica') {
                    $displayTone = (!empty($item['custom_tone'])) ? $item['custom_tone'] : ($item['song_tone'] ?? null);
                }
            ?>
            <div class="roteiro-view-item">
                <div class="roteiro-view-num"><?= $idx + 1 ?></div>
                <div class="roteiro-view-icon">
                    <i data-lucide="<?= $icon ?>" width="18"></i>
                </div>
                <div class="roteiro-view-info">
                    <div class="roteiro-view-title"><?= htmlspecialchars($displayTitle) ?></div>
                    <div class="roteiro-view-meta">
                        <span><?= htmlspecialchars($label) ?></span>
                        <?php if ($item['item_type'] === 'musica' && $item['song_artist']): ?>
                            <span><?= htmlspecialchars($item['song_artist']) ?></span>
                        <?php endif; ?>
                        <?php if ($displayTone): ?>
                            <span class="roteiro-view-tone"><?= htmlspecialchars($displayTone) ?></span>
                        <?php endif; ?>
                    </div>
                    <?php
                    // nota_interna visível APENAS para admin (ROT-04 — músico não vê)
                    if (!empty($item['nota_interna']) && $_SESSION['user_role'] === 'admin'):
                    ?>
                    <div class="roteiro-view-nota">
                        <i data-lucide="eye-off" width="12" style="flex-shrink:0;margin-top:2px;"></i>
                        <span><?= htmlspecialchars($item['nota_interna']) ?></span>
                    </div>
                    <?php endif; ?>
                </div>
            </div>
            <?php endforeach; ?>
        </div>
        <?php endif; ?>
```
  </action>

  <verify>
    <automated>php -l admin/escala_detalhe.php</automated>
  </verify>

  <acceptance_criteria>
    - `php -l admin/escala_detalhe.php` — sem erros de sintaxe
    - Contém `class="roteiro-view-section"` dentro do bloco else (view mode)
    - Contém `count($roteiro)` para exibir número de itens no header
    - Contém lógica de `$displayTone` com prioridade: custom_tone > song_tone
    - Contém `htmlspecialchars($displayTitle)`, `htmlspecialchars($displayTone)` e `htmlspecialchars($item['nota_interna'])`
    - nota_interna está dentro de `if (!empty($item['nota_interna']) && $_SESSION['user_role'] === 'admin')`
    - Seção inteira está dentro de `if (!empty($roteiro))` (não aparece quando roteiro está vazio)
    - Ícone Lucide por tipo (`data-lucide="<?= $icon ?>"`)
    - Seção está APÓS repertório e ANTES de comentários no flow do HTML
  </acceptance_criteria>

  <done>
Seção de view do roteiro adicionada ao modo view de escala_detalhe.php. Músico vê itens ordenados com ícone, tipo, nome da música e tom. nota_interna oculta para músicos, visível para admin.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PHP → HTML render | Dados do banco renderizados para o browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03C-01 | Information Disclosure | nota_interna no view | mitigate | Condicional `$_SESSION['user_role'] === 'admin'` no PHP — servidor decide, não o cliente |
| T-03C-02 | XSS | renderização de dados do banco | mitigate | Todos os campos passam por `htmlspecialchars()` antes de inserir no HTML |
| T-03C-03 | Injection | query $stmtRoteiro | mitigate | PDO prepared statement com `?` placeholder — sem interpolação de SQL |
</threat_model>

<verification>
1. `php -l admin/escala_detalhe.php` — sem erros de sintaxe PHP
2. `grep "roteiro-view-section" admin/escala_detalhe.php` — encontrado
3. `grep "user_role.*admin" admin/escala_detalhe.php` — linha com nota_interna encontrada
4. `grep "htmlspecialchars.*nota_interna\|nota_interna.*htmlspecialchars" admin/escala_detalhe.php` — sanitização presente
5. Testar manualmente como músico (user): acessar escala com roteiro → seção aparece → nota_interna NÃO visível
6. Testar manualmente como admin: mesma escala → nota_interna aparece em azul
7. Testar: músicas com custom_tone → exibe custom_tone; músicas sem custom_tone → exibe song_tone
8. Testar: escala sem roteiro → seção não aparece
</verification>

<success_criteria>
- Seção "Roteiro de Culto" visível no modo view quando há itens
- Itens em ordem correta (order_position ASC)
- Número de itens no header (badge section-count)
- Ícone Lucide correto por tipo de item
- Tom customizado exibido como badge azul — custom_tone prioridade sobre song_tone (ROT-05)
- nota_interna visível para admin, invisível para músico (server-side — não só CSS)
- Seção oculta quando roteiro está vazio (não exibe seção vazia)
- Todos os dados sanitizados com htmlspecialchars (sem XSS)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-roteiro/03-03-SUMMARY.md`
</output>
