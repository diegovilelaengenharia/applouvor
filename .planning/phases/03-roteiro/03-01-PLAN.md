---
phase: 03-roteiro
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/003_schedule_roteiro.sql
  - api/roteiro.php
autonomous: true
requirements:
  - ROT-01
  - ROT-02

must_haves:
  truths:
    - "Tabela schedule_roteiro existe com colunas: id, schedule_id, order_position, item_type, title, song_id, custom_tone, nota_interna, created_at"
    - "item_type é ENUM com valores: musica, oracao, palavra, anuncio, intervalo, livre"
    - "Foreign key schedule_id → schedules(id) ON DELETE CASCADE"
    - "Foreign key song_id → songs(id) ON DELETE SET NULL (nullable)"
    - "api/roteiro.php responde GET com lista de itens em JSON"
    - "api/roteiro.php aceita POST com action=add e insere item ao final do roteiro"
    - "api/roteiro.php aceita POST com action=delete e remove item (valida admin)"
    - "api/roteiro.php aceita POST com action=reorder e atualiza order_position de múltiplos itens"
    - "API valida sessão — retorna 401 se não autenticado"
    - "API valida que schedule_id pertence a uma escala real antes de qualquer operação de escrita"
  artifacts:
    - path: "database/migrations/003_schedule_roteiro.sql"
      provides: "Migration SQL para criação da tabela schedule_roteiro"
      contains: "CREATE TABLE schedule_roteiro"
    - path: "api/roteiro.php"
      provides: "CRUD API para itens do roteiro"
      contains: "action.*add.*delete.*reorder"
  key_links:
    - from: "api/roteiro.php"
      to: "schedule_roteiro (table)"
      via: "PDO queries"
      pattern: "schedule_roteiro"
---

<objective>
Criar a infraestrutura de dados e API para o Roteiro de Culto: migration SQL da tabela `schedule_roteiro` e endpoint `api/roteiro.php` com operações de listagem, adição, deleção e reordenação de itens.

Purpose: Base necessária para os planos 03-02 e 03-03. Sem a tabela e API, a UI não tem onde persistir os dados.
Output: `database/migrations/003_schedule_roteiro.sql` + `api/roteiro.php` funcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-roteiro/03-CONTEXT.md

<interfaces>
De includes/db.php:
- Expõe `$pdo` como instância PDO já conectada ao banco
- Requer: `require_once '../includes/db.php';`

De includes/auth.php:
- `checkLogin()` — redireciona para login se não autenticado
- `$_SESSION['user_id']` — ID do usuário logado
- `$_SESSION['user_role']` — 'admin' ou 'user'
- Requer: `require_once '../includes/auth.php';`

Banco existente (relevante):
- `schedules(id)` — tabela pai; CASCADE delete propaga ao roteiro
- `songs(id, title, artist, tone)` — referenciada por song_id (SET NULL se deletar música)

Padrão de API do projeto (ver api/confirm_scale.php):
- `header('Content-Type: application/json');` no topo
- Resposta: `json_encode(['success' => bool, 'data' => ..., 'message' => '...'])`
- Validação de sessão via `$_SESSION['user_id']`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration SQL da tabela schedule_roteiro</name>
  <files>database/migrations/003_schedule_roteiro.sql</files>

  <read_first>
    - database/migrations/add_dashboard_settings.sql — ver padrão de migration adotado no projeto (IF NOT EXISTS, ENGINE, CHARSET)
  </read_first>

  <action>
Criar `database/migrations/003_schedule_roteiro.sql` com o seguinte conteúdo:

```sql
-- Migration 003: Tabela para o Roteiro de Culto
-- Cada escala pode ter um roteiro ordenado com itens de tipos diferentes.
-- song_id é NULL para itens não-musicais (oração, palavra, etc.)
-- nota_interna é visível apenas para admin — não expor na view do músico.

CREATE TABLE IF NOT EXISTS schedule_roteiro (
    id INT AUTO_INCREMENT PRIMARY KEY,
    schedule_id INT NOT NULL,
    order_position SMALLINT UNSIGNED NOT NULL DEFAULT 0,
    item_type ENUM('musica','oracao','palavra','anuncio','intervalo','livre') NOT NULL DEFAULT 'musica',
    title VARCHAR(255) NULL COMMENT 'Título do item (obrigatório para não-músicas; opcional para músicas — usa title da songs)',
    song_id INT NULL COMMENT 'FK para songs. NULL se item_type != musica',
    custom_tone VARCHAR(10) NULL COMMENT 'Tom customizado para esta escala. NULL = usar songs.tone padrão',
    nota_interna TEXT NULL COMMENT 'Nota visível apenas para admin (líder). Nunca exibir para músicos.',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    KEY idx_schedule_order (schedule_id, order_position),
    CONSTRAINT fk_roteiro_schedule FOREIGN KEY (schedule_id) REFERENCES schedules(id) ON DELETE CASCADE,
    CONSTRAINT fk_roteiro_song FOREIGN KEY (song_id) REFERENCES songs(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```
  </action>

  <verify>
    <automated>php -r "echo file_exists('database/migrations/003_schedule_roteiro.sql') ? 'OK' : 'MISSING';"</automated>
  </verify>

  <acceptance_criteria>
    - Arquivo `database/migrations/003_schedule_roteiro.sql` existe
    - Contém `CREATE TABLE IF NOT EXISTS schedule_roteiro`
    - Contém ENUM com 6 valores: musica, oracao, palavra, anuncio, intervalo, livre
    - Contém `song_id INT NULL`
    - Contém `custom_tone VARCHAR(10) NULL`
    - Contém `nota_interna TEXT NULL`
    - Contém FK para schedules com ON DELETE CASCADE
    - Contém FK para songs com ON DELETE SET NULL
    - Contém índice em (schedule_id, order_position)
  </acceptance_criteria>

  <done>Migration SQL criada em database/migrations/003_schedule_roteiro.sql.</done>
</task>

<task type="auto">
  <name>Task 2: Criar api/roteiro.php com CRUD completo</name>
  <files>api/roteiro.php</files>

  <read_first>
    - api/confirm_scale.php — ver padrão completo de API do projeto (auth, JSON, PDO, tratamento de erro)
    - api/send_reminders.php — ver exemplo de API com operações mais complexas e validação de admin
  </read_first>

  <action>
Criar `api/roteiro.php` com o seguinte conteúdo:

```php
<?php
// api/roteiro.php — CRUD para itens do roteiro de culto
header('Content-Type: application/json');
require_once '../includes/auth.php';
require_once '../includes/db.php';

$userId   = $_SESSION['user_id']   ?? 0;
$userRole = $_SESSION['user_role'] ?? '';

if (!$userId) {
    http_response_code(401);
    echo json_encode(['success' => false, 'message' => 'Não autenticado']);
    exit;
}

$method = $_SERVER['REQUEST_METHOD'];

// ── GET: listar itens do roteiro ──────────────────────────────────────────────
if ($method === 'GET') {
    $scheduleId = (int)($_GET['schedule_id'] ?? 0);
    if (!$scheduleId) {
        echo json_encode(['success' => false, 'message' => 'schedule_id obrigatório']);
        exit;
    }

    try {
        $stmt = $pdo->prepare("
            SELECT r.id, r.order_position, r.item_type, r.title,
                   r.song_id, r.custom_tone, r.nota_interna,
                   s.title as song_title, s.artist as song_artist, s.tone as song_tone
            FROM schedule_roteiro r
            LEFT JOIN songs s ON s.id = r.song_id
            WHERE r.schedule_id = ?
            ORDER BY r.order_position ASC, r.id ASC
        ");
        $stmt->execute([$scheduleId]);
        $items = $stmt->fetchAll(PDO::FETCH_ASSOC);

        // nota_interna só retorna para admin
        if ($userRole !== 'admin') {
            foreach ($items as &$item) {
                $item['nota_interna'] = null;
            }
            unset($item);
        }

        echo json_encode(['success' => true, 'data' => $items]);
    } catch (Exception $e) {
        http_response_code(500);
        echo json_encode(['success' => false, 'message' => 'Erro ao buscar roteiro']);
    }
    exit;
}

// ── POST: add / delete / reorder ──────────────────────────────────────────────
if ($method === 'POST') {
    // Somente admin pode modificar o roteiro
    if ($userRole !== 'admin') {
        http_response_code(403);
        echo json_encode(['success' => false, 'message' => 'Apenas administradores podem editar o roteiro']);
        exit;
    }

    $data   = json_decode(file_get_contents('php://input'), true) ?? [];
    $action = $data['action'] ?? '';

    // Validar que schedule existe (guard comum a todas as ações de escrita)
    $scheduleId = (int)($data['schedule_id'] ?? 0);
    if ($scheduleId) {
        $chk = $pdo->prepare("SELECT id FROM schedules WHERE id = ?");
        $chk->execute([$scheduleId]);
        if (!$chk->fetch()) {
            echo json_encode(['success' => false, 'message' => 'Escala não encontrada']);
            exit;
        }
    }

    // ── action: add ──────────────────────────────────────────────────────────
    if ($action === 'add') {
        if (!$scheduleId) {
            echo json_encode(['success' => false, 'message' => 'schedule_id obrigatório']);
            exit;
        }

        $validTypes = ['musica', 'oracao', 'palavra', 'anuncio', 'intervalo', 'livre'];
        $itemType   = in_array($data['item_type'] ?? '', $validTypes) ? $data['item_type'] : 'livre';
        $title      = isset($data['title'])        ? trim(substr($data['title'], 0, 255))       : null;
        $songId     = ($itemType === 'musica' && !empty($data['song_id'])) ? (int)$data['song_id'] : null;
        $customTone = ($itemType === 'musica' && !empty($data['custom_tone'])) ? trim(substr($data['custom_tone'], 0, 10)) : null;
        $notaInterna = isset($data['nota_interna']) ? trim($data['nota_interna']) : null;
        $notaInterna = $notaInterna === '' ? null : $notaInterna;

        // Validar song_id existe (se fornecido)
        if ($songId) {
            $sc = $pdo->prepare("SELECT id FROM songs WHERE id = ?");
            $sc->execute([$songId]);
            if (!$sc->fetch()) $songId = null;
        }

        // Próxima posição = max atual + 1
        $posStmt = $pdo->prepare("SELECT COALESCE(MAX(order_position), -1) + 1 FROM schedule_roteiro WHERE schedule_id = ?");
        $posStmt->execute([$scheduleId]);
        $nextPos = (int)$posStmt->fetchColumn();

        try {
            $ins = $pdo->prepare("
                INSERT INTO schedule_roteiro
                    (schedule_id, order_position, item_type, title, song_id, custom_tone, nota_interna)
                VALUES (?, ?, ?, ?, ?, ?, ?)
            ");
            $ins->execute([$scheduleId, $nextPos, $itemType, $title ?: null, $songId, $customTone, $notaInterna]);
            $newId = (int)$pdo->lastInsertId();
            echo json_encode(['success' => true, 'id' => $newId, 'order_position' => $nextPos]);
        } catch (Exception $e) {
            http_response_code(500);
            echo json_encode(['success' => false, 'message' => 'Erro ao adicionar item']);
        }
        exit;
    }

    // ── action: delete ────────────────────────────────────────────────────────
    if ($action === 'delete') {
        $itemId = (int)($data['id'] ?? 0);
        if (!$itemId) {
            echo json_encode(['success' => false, 'message' => 'id do item obrigatório']);
            exit;
        }

        try {
            // Verificar que o item pertence a uma escala válida (scheduleId já validado acima se fornecido)
            $del = $pdo->prepare("DELETE FROM schedule_roteiro WHERE id = ?");
            $del->execute([$itemId]);
            if ($del->rowCount() === 0) {
                echo json_encode(['success' => false, 'message' => 'Item não encontrado']);
                exit;
            }
            echo json_encode(['success' => true]);
        } catch (Exception $e) {
            http_response_code(500);
            echo json_encode(['success' => false, 'message' => 'Erro ao deletar item']);
        }
        exit;
    }

    // ── action: reorder ───────────────────────────────────────────────────────
    if ($action === 'reorder') {
        $items = $data['items'] ?? [];
        if (empty($items) || !is_array($items)) {
            echo json_encode(['success' => false, 'message' => 'items[] obrigatório']);
            exit;
        }

        try {
            $upd = $pdo->prepare("UPDATE schedule_roteiro SET order_position = ? WHERE id = ?");
            foreach ($items as $item) {
                $iId  = (int)($item['id']  ?? 0);
                $iPos = (int)($item['pos'] ?? 0);
                if ($iId > 0) $upd->execute([$iPos, $iId]);
            }
            echo json_encode(['success' => true]);
        } catch (Exception $e) {
            http_response_code(500);
            echo json_encode(['success' => false, 'message' => 'Erro ao reordenar']);
        }
        exit;
    }

    echo json_encode(['success' => false, 'message' => 'action inválida']);
    exit;
}

echo json_encode(['success' => false, 'message' => 'Método não permitido']);
```
  </action>

  <verify>
    <automated>php -l api/roteiro.php</automated>
  </verify>

  <acceptance_criteria>
    - `php -l api/roteiro.php` retorna "No syntax errors detected"
    - Arquivo contém `header('Content-Type: application/json')`
    - Contém verificação de sessão com retorno 401 se não autenticado
    - Contém verificação de `$userRole !== 'admin'` com retorno 403 para ações de escrita
    - GET retorna lista de itens com JOIN em songs
    - GET oculta nota_interna para não-admins (atribui null)
    - action=add valida item_type contra whitelist de 6 valores
    - action=add valida song_id contra tabela songs antes de inserir
    - action=add calcula next order_position como MAX+1
    - action=delete usa rowCount() para detectar item não encontrado
    - action=reorder aceita array de {id, pos} e atualiza em loop
    - Não expõe mensagens de erro PDO (apenas "Erro ao...")
  </acceptance_criteria>

  <done>
API api/roteiro.php criada com GET (lista) + POST action=add/delete/reorder. Admin-only para escrita. nota_interna filtrada para músicos no GET.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → api/roteiro.php (GET) | Usuário autenticado busca roteiro — nota_interna filtrada para não-admin |
| Browser → api/roteiro.php (POST) | Apenas admin pode add/delete/reorder |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03A-01 | Spoofing | api/roteiro.php | mitigate | Verificação de `$_SESSION['user_id']` no início; 401 se não autenticado |
| T-03A-02 | Elevation | api/roteiro.php (POST) | mitigate | `$userRole !== 'admin'` retorna 403 antes de qualquer escrita |
| T-03A-03 | Tampering | action=add item_type | mitigate | Whitelist de 6 valores válidos — valor fora da lista vira 'livre' |
| T-03A-04 | Information Disclosure | nota_interna no GET | mitigate | nota_interna atribuída null para `user_role !== 'admin'` antes de json_encode |
| T-03A-05 | Tampering | action=add song_id | mitigate | Valida song_id contra tabela songs — ID inválido resulta em song_id=null |
</threat_model>

<verification>
1. `php -l api/roteiro.php` — sem erros de sintaxe
2. `php -l database/migrations/003_schedule_roteiro.sql` — N/A (SQL puro, não PHP)
3. `grep "nota_interna" api/roteiro.php` — deve aparecer o filtro para não-admin
4. `grep "whitelist\|validTypes\|in_array" api/roteiro.php` — validação do item_type existe
5. Executar migration no banco local: `mysql -u root pibo_louvor < database/migrations/003_schedule_roteiro.sql`
6. Testar GET: `curl http://localhost:8080/api/roteiro.php?schedule_id=1` (com sessão) — retorna JSON com data:[]
</verification>

<success_criteria>
- Migration SQL criada e executável no banco local
- Tabela schedule_roteiro criada com todas as colunas e FKs corretas
- api/roteiro.php sem erros de sintaxe PHP
- GET retorna itens em ordem com JOIN em songs
- nota_interna filtrada para músicos no GET
- POST add insere item validando item_type e song_id
- POST delete remove item com verificação de existência
- POST reorder atualiza order_position em batch
- Admin-only para todas as operações POST (403 para user)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-roteiro/03-01-SUMMARY.md`
</output>
