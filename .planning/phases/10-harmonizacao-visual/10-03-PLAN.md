---
phase: 10-harmonizacao-visual
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - assets/css/components/buttons.css
  - assets/css/components/action-buttons.css
  - assets/css/components/page-sub-header.css
  - assets/css/pages/oracao.css
  - assets/css/pages/devocionais.css
  - assets/css/pages/escalas.css
  - assets/css/pages/lider.css
  - assets/css/pages/relatorios.css
  - includes/layout.php
  - admin/index.php
  - app/index.php
autonomous: true
requirements: [UI-02, UI-03]

must_haves:
  truths:
    - "buttons.css has an active/press scale(0.97) state and a focus-visible outline (WCAG AA)"
    - "Page-level button classes (.pray-btn, .btn-new-prayer, .btn-print) are removed from CSS and their callers use .btn .btn-* matrix classes"
    - "border-radius literals in oracao/devocionais/escalas/lider are replaced with --radius-* tokens"
    - ".fab-create exists in one place (action-buttons.css) and is removed from oracao.css and devocionais.css"
    - "The page-sub-header is hidden on the two dashboards via body.page-is-dashboard"
  artifacts:
    - path: "assets/css/components/buttons.css"
      provides: "Button primitive with active + focus-visible states"
      contains: "scale(0.97)"
    - path: "assets/css/components/action-buttons.css"
      provides: "Single canonical .fab-create"
      contains: ".fab-create"
    - path: "assets/css/components/page-sub-header.css"
      provides: "Dashboard hide rule"
      contains: "body.page-is-dashboard"
    - path: "includes/layout.php"
      provides: "page-is-dashboard body class on home"
      contains: "page-is-dashboard"
  key_links:
    - from: "admin/index.php / app/index.php"
      to: "page-sub-header.css body.page-is-dashboard rule"
      via: "body class set in renderAppHeader when $isHome"
      pattern: "page-is-dashboard"
    - from: "oracao.css / devocionais.css callers"
      to: "components/action-buttons.css .fab-create"
      via: "single shared FAB class"
      pattern: "fab-create"
---

<objective>
Standardise the button system and finish the density/radius tokenisation. Three coordinated changes:
1. Add the missing `:active` (scale 0.97) and `:focus-visible` (3px primary outline) states to `buttons.css` so every button gets tactile + accessible feedback.
2. Remove page-level button classes that bypass the matrix (`.pray-btn`, `.btn-new-prayer`, `.btn-print`) and point their callers at `.btn .btn-*` classes; replace `border-radius: Npx` literals with `--radius-*` tokens in oracao/devocionais/escalas/lider.
3. Hide the page-sub-header on the two dashboards (`admin/index.php`, `app/index.php`) via a `body.page-is-dashboard` class, and deduplicate `.fab-create` into `action-buttons.css`.

Depends on Plan 02 — typography is settled, so button font-sizes won't shift under us.

Purpose: 44px touch targets everywhere, an 8-12px radius scale, one primary/secondary/ghost hierarchy, a single FAB definition, and a clean dashboard chrome.
Output: A locked button matrix with full states, token-based radii on the four priority pages, one FAB source, and dashboard headers hidden.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-harmonizacao-visual/10-UI-SPEC.md
@.planning/phases/10-harmonizacao-visual/10-RESEARCH.md
@.planning/phases/10-harmonizacao-visual/10-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Radius + button tokens from design-system.css. Use verbatim. -->
--radius-sm: 6px;  --radius-md: 8px;  --radius-lg: 12px;  --radius-xl: 16px;  --radius-2xl: 24px;  --radius-full: 9999px;
--primary: var(--blue-500);   --color-primary: var(--blue-500);
--color-accent: var(--yellow-500);   --color-success: var(--green-500);   --color-danger: var(--red-500);
<!-- buttons.css ALREADY has: .btn min-height:44px radius-lg; .btn-sm min-height:36px radius-md; .btn-lg min-height:52px radius-xl; .btn-icon 44x44; .btn-primary/.btn-accent/.btn-secondary/.btn-ghost/.btn-success/.btn-danger; :hover translateY(-1px). MISSING: :active scale(0.97), :focus-visible outline. -->
<!-- radius literal -> token: 6px->sm  8px->md  10px/14px->md or lg(nearest)  12px->lg  16px->xl  24px->2xl  20px+/9999px/50%(pill)->full -->
<!-- layout.php: <body> opened at line ~76 inside renderAppHeader(); $isHome is NOT computed there (only in renderPageHeader line ~228). Must add $isHome = basename($_SERVER['PHP_SELF']) === 'index.php'; inside renderAppHeader before the body tag. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete the button matrix — add :active and :focus-visible states</name>
  <read_first>
    - assets/css/components/buttons.css (full — confirm size matrix lines 15/17/109/111/113/116/118/120/123/126/130/133 and existing :hover translateY rules at 43/45/55/57; locate end of file for appending shared state rules)
    - assets/css/design-system.css (confirm --primary, --shadow-md token names)
    - .planning/phases/10-harmonizacao-visual/10-UI-SPEC.md (§Button System Contract — Size Matrix + States table)
  </read_first>
  <action>
    In `assets/css/components/buttons.css`, add the two missing shared states from the UI-SPEC §States table. Append (or place near the existing `.btn` base rule) generic rules that apply to all button variants:
    - Active/press: `.btn:active:not(:disabled) { transform: scale(0.97); }`
    - Focus accessibility: `.btn:focus-visible { outline: 3px solid var(--primary); outline-offset: 2px; }`
    - Disabled (if not already present): `.btn:disabled, .btn[disabled] { opacity: 0.5; cursor: not-allowed; }` and ensure disabled buttons get no hover lift (`.btn:disabled:hover { transform: none; box-shadow: none; }`).
    Do NOT change the existing size matrix (min-height 44/36/52, radius-lg/md/xl) — RESEARCH confirms it is already correct. Do NOT change existing :hover translateY rules. Keep additions minimal and applied to the base `.btn` so all variants inherit.
  </action>
  <verify>
    <automated>cd "$PWD"; grep -cE 'scale\(0\.97\)' assets/css/components/buttons.css; grep -cE 'focus-visible' assets/css/components/buttons.css; grep -cE 'min-height:\s*44px' assets/css/components/buttons.css</automated>
  </verify>
  <acceptance_criteria>
    - buttons.css contains `scale(0.97)` on a `.btn:active` rule (grep -cE 'scale\(0\.97\)' returns >= 1)
    - buttons.css contains a `:focus-visible` rule with `outline: 3px solid var(--primary)` (grep -cE 'focus-visible' returns >= 1)
    - buttons.css still has `.btn { ... min-height: 44px; ... }` (grep -cE 'min-height:\s*44px' returns >= 1 — matrix unchanged)
    - buttons.css `.btn` border-radius still references `var(--radius-lg)` (grep finds it — not regressed to a literal)
  </acceptance_criteria>
  <done>The button primitive has tactile press feedback (scale 0.97) and a WCAG-AA focus ring; the existing 44px/radius matrix is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Remove page-level button classes + tokenise radius (oracao, devocionais, escalas, lider, relatorios)</name>
  <read_first>
    - assets/css/pages/oracao.css (full — locate .pray-btn, .btn-new-prayer rules + all border-radius: Npx literals)
    - assets/css/pages/devocionais.css (full — border-radius literals)
    - assets/css/pages/escalas.css (full — border-radius literals; .scale-card::before 5px accent strip)
    - assets/css/pages/lider.css (full — border-radius literals on action-cards/kpi-cards)
    - assets/css/pages/relatorios.css (full — locate .btn-print rule)
    - admin/oracao.php, admin/devocionais.php, admin/relatorios_gerais.php (locate HTML elements using class="pray-btn"/"btn-new-prayer"/"btn-print" to repoint callers)
    - assets/css/design-system.css (confirm --radius-* token names)
    - .planning/phases/10-harmonizacao-visual/10-UI-SPEC.md (§Button System "Prohibited page-level button patterns" + §Redundant Declarations Low border-radius table + §.pib-card accent strip rule)
  </read_first>
  <action>
    BUTTON CLASS REMOVAL (UI-02). For each prohibited class, first repoint its HTML callers, THEN delete the CSS rule:
    - `.pray-btn` in oracao.css -> callers in admin/oracao.php become `class="btn btn-primary"` (active/primary action) or `class="btn btn-ghost"` (inactive). Then delete the `.pray-btn` CSS block.
    - `.btn-new-prayer` in oracao.css -> caller becomes `class="btn btn-primary btn-block"`. Delete the CSS block.
    - `.btn-print` in relatorios.css -> caller in admin/relatorios_gerais.php becomes `class="btn btn-secondary"`. Delete the CSS block.
    - `.btn-registrar-faltas` in escalas.css: KEEP (UI-SPEC permits — it uses tokens for semantic context color). Do not remove.
    - Remove any `border-radius: 20px`/`24px` on regular buttons (pill is only for `.btn-full-pill`/FAB).
    RADIUS TOKENISATION (UI-03). In oracao.css, devocionais.css, escalas.css, lider.css, replace raw `border-radius: Npx` per the map: 6px->var(--radius-sm), 8px->var(--radius-md), 10px->var(--radius-md), 12px->var(--radius-lg), 14px->var(--radius-lg), 16px->var(--radius-xl), 24px->var(--radius-2xl), pill(9999px/50%/20px+ on rounded chips)->var(--radius-full). In escalas.css, normalise the `.scale-card::before` accent strip from 5px to 4px per UI-SPEC accent strip rule.
    Do NOT add `!important`. Do NOT touch relatorios.css radius beyond the .btn-print removal (radius tokenisation there is deferred). Do NOT modify dashboard.css/dark-mode.css/roles.css.
  </action>
  <verify>
    <automated>cd "$PWD"; grep -cE '\.pray-btn|\.btn-new-prayer' assets/css/pages/oracao.css; grep -cE '\.btn-print' assets/css/pages/relatorios.css; grep -ciE 'class="[^"]*pray-btn|class="[^"]*btn-print' admin/oracao.php admin/relatorios_gerais.php</automated>
  </verify>
  <acceptance_criteria>
    - oracao.css contains no `.pray-btn` or `.btn-new-prayer` selector (grep -cE returns 0)
    - relatorios.css contains no `.btn-print` selector (grep -cE returns 0)
    - admin/oracao.php and admin/relatorios_gerais.php contain no `class="...pray-btn..."` / `class="...btn-print..."` (grep -ciE returns 0 — callers migrated)
    - oracao.php pray buttons now carry `btn btn-primary` or `btn btn-ghost`; relatorios print button carries `btn btn-secondary` (grep confirms `btn btn-` present)
    - escalas.css `.scale-card::before` uses `4px` accent strip (no `5px` accent strip remains)
    - oracao.css/devocionais.css/escalas.css/lider.css: no regular-button `border-radius: 20px`/`24px` literal remains (pill stays only via var(--radius-full))
    - At least one `var(--radius-` token reference now present in each of oracao.css, devocionais.css, escalas.css, lider.css
  </acceptance_criteria>
  <done>Prohibited page button classes removed and callers migrated to the matrix; radii tokenised on the four priority pages; accent strip normalised to 4px.</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard header hide + FAB dedup + Wave 3 regression sweep</name>
  <read_first>
    - includes/layout.php (lines ~26 renderAppHeader signature, ~76 <body> tag, ~228 renderPageHeader $isHome computation — confirm $isHome is NOT in scope in renderAppHeader)
    - admin/index.php (confirm it calls renderAppHeader / how body is opened)
    - app/index.php (same)
    - assets/css/components/page-sub-header.css (end of file — append target for the hide rule; confirm no existing display:none on .page-sub-header)
    - assets/css/components/action-buttons.css (full — confirm .fab-create is NOT yet declared; this is the dedup destination)
    - assets/css/pages/oracao.css and devocionais.css (locate the duplicate .fab-create rules to move/remove)
    - .planning/phases/10-harmonizacao-visual/10-UI-SPEC.md (§Header Behaviour Rules dashboard hide + §Redundant Declarations Low ".fab-create duplicate")
    - .planning/phases/10-harmonizacao-visual/10-RESEARCH.md (§Pattern Dashboard Header Hide + §Pitfall 5 FAB dedup specificity)
  </read_first>
  <action>
    DASHBOARD HEADER HIDE (UI-03):
    - In `includes/layout.php`, inside `renderAppHeader()` BEFORE the `<body>` tag (~line 76), add `$isHome = basename($_SERVER['PHP_SELF']) === 'index.php';`. Change `<body>` to `<body<?php if ($isHome): ?> class="page-is-dashboard"<?php endif; ?>>` so admin/index.php and app/index.php (both `index.php`) get the class. (RESEARCH Open Question #3 confirms $isHome is not yet computed in this function — you must add it.)
    - Append to `assets/css/components/page-sub-header.css`: `body.page-is-dashboard .page-sub-header { display: none; }`.
    - This is reversible and zero-risk; the header's user-profile-pill/theme controls remain available via sidebar (desktop) / bottom-nav (mobile) per UI-SPEC.
    FAB DEDUP (UI-03):
    - Copy the canonical `.fab-create { ... }` declaration into `assets/css/components/action-buttons.css` (it is currently NOT there — confirmed). Then DELETE the `.fab-create` rule from BOTH oracao.css and devocionais.css entirely (no commented-out leftovers — per Pitfall #5). action-buttons.css loads earlier in the cascade than page CSS, so after this the single definition wins for both pages; verify visually the FAB color/size is unchanged on both.
    REGRESSION SWEEP (UI-02/UI-03):
    - With localhost:8080 running, audit at 375px + 1280px + dark@375px: admin/index.php and app/index.php (header hidden), oracao.php + devocionais.php (FAB present once, pray buttons are .btn matrix), relatorios_gerais.php (print is .btn-secondary), escalas.php + lider.php (radii consistent). Apply the 5-point checklist; specifically confirm: buttons >= 44px height with 8-12px radius; FAB renders identically on both pages; dashboards show no page-sub-header; focus ring visible on keyboard-tab.
  </action>
  <verify>
    <automated>cd "$PWD"; grep -cE 'page-is-dashboard' includes/layout.php; grep -cE 'body\.page-is-dashboard' assets/css/components/page-sub-header.css; grep -cE 'fab-create' assets/css/components/action-buttons.css; grep -cE 'fab-create' assets/css/pages/oracao.css assets/css/pages/devocionais.css</automated>
  </verify>
  <acceptance_criteria>
    - includes/layout.php contains `page-is-dashboard` and `$isHome = basename(` inside renderAppHeader (grep -cE 'page-is-dashboard' returns >= 1)
    - page-sub-header.css contains `body.page-is-dashboard .page-sub-header { display: none; }` (grep -cE 'body\.page-is-dashboard' returns >= 1)
    - action-buttons.css contains a `.fab-create` rule (grep -cE 'fab-create' returns >= 1)
    - oracao.css and devocionais.css contain NO `.fab-create` rule (combined grep -cE 'fab-create' returns 0 for both)
    - Route audit: admin/index.php, app/index.php, oracao.php, devocionais.php, relatorios_gerais.php, escalas.php, lider.php each return HTTP 200 with no PHP error text
    - SUMMARY confirms: dashboards have no page-sub-header; FAB identical on both pages; buttons >= 44px with 8-12px radius; focus ring visible
  </acceptance_criteria>
  <done>Dashboard headers hidden via body class; single FAB source in action-buttons.css; Wave 3 routes return clean HTTP 200 and pass the button/radius/header checklist.</done>
</task>

</tasks>

<threat_model>
No new attack surface — CSS/presentation-only changes; the only PHP edit is adding a `$isHome` boolean and a body CSS class (no input handling, auth, or data flow). No threats at ASVS L1. The non-security risk is visual regression (FAB specificity per RESEARCH Pitfall #5, dashboard header accidentally hidden on non-home pages), mitigated by the Task 3 regression sweep in 10-VALIDATION.md.
</threat_model>

<verification>
- buttons.css has :active scale(0.97) + :focus-visible outline; matrix unchanged
- .pray-btn/.btn-new-prayer/.btn-print removed and callers migrated to .btn matrix
- Radii tokenised in oracao/devocionais/escalas/lider; accent strip 4px
- Single .fab-create in action-buttons.css; removed from both page files
- body.page-is-dashboard hides header on both dashboards; routes clean HTTP 200
</verification>

<success_criteria>
Buttons share one matrix with 44px touch targets, 8-12px radii, full hover/active/focus/disabled states, and a consistent primary/secondary/ghost hierarchy with no page-level bypass classes. Radii on the four priority pages are token-based, the FAB has one source, and the dashboards present clean chrome. (UI-02, UI-03)
</success_criteria>

<output>
After completion, create `.planning/phases/10-harmonizacao-visual/10-03-SUMMARY.md`
</output>
