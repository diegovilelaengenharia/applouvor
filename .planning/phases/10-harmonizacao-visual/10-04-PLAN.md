---
phase: 10-harmonizacao-visual
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified: []
autonomous: false
requirements: [UI-04]

must_haves:
  truths:
    - "Every admin/ and app/ route returns HTTP 200 with no PHP error / SQLSTATE / Fatal error in the body"
    - "No route has horizontal scroll / element overflow at 375px"
    - "Text is legible in both light and dark mode on every audited route (no white-on-white / dark-on-dark)"
    - "Buttons are >= 44px touch height with 8-12px radius across the app"
    - "Typography matches the canonical scale — no oversized/undersized headings vs siblings"
    - "Diego confirms the overall visual harmony of the 375px + desktop screenshot set"
  artifacts:
    - path: ".planning/phases/10-harmonizacao-visual/10-04-SUMMARY.md"
      provides: "Recorded audit results — 24-screenshot pass + 5-point checklist per route"
  key_links:
    - from: "verify_all_pages.py (Playwright runner)"
      to: "all admin/ + app/ routes"
      via: "HTTP 200 + PHP-error scan + screenshots at 375/1280 + dark@375"
      pattern: "status.*ok|HTTP 200"
---

<objective>
Phase verification gate (UI-04). Run the full visual + non-regression audit across ALL admin/ and app/ routes at 375px, 1280px, and dark-mode@375px, applying the 5-point regression checklist from 10-VALIDATION.md. This wave produces NO code changes — it proves the cumulative result of Waves 1-3 is consistent and regression-free, then captures Diego's subjective sign-off on harmony.

Depends on all prior plans (01, 02, 03).

Purpose: Confirm the harmonisation goal is observably achieved — no PHP errors, no mobile overflow, dark-mode parity, 44px buttons, canonical type scale — before closing the phase.
Output: A SUMMARY recording the 24-screenshot audit results, the 5-point checklist per critical route, any defects found + fixed, and Diego's harmony sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-harmonizacao-visual/10-UI-SPEC.md
@.planning/phases/10-harmonizacao-visual/10-RESEARCH.md
@.planning/phases/10-harmonizacao-visual/10-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Validation tooling (from 10-VALIDATION.md / 10-RESEARCH.md Validation Architecture) -->
Playwright 1.60.0 (Python). Runner: .agent/skills/webapp-testing/scripts/playwright_runner.py
Aggregate audit (if present): python verify_all_pages.py --screenshots --viewports 375,1280 --dark
Per-route: python .agent/skills/webapp-testing/scripts/playwright_runner.py http://localhost:8080/applouvor/<path> --screenshot
Local server: php -S localhost:8080 via run_server.bat (MUST be running first)
<!-- 8 critical routes: admin/index.php, admin/escalas.php, admin/escala_detalhe.php, admin/repertorio.php, admin/oracao.php, admin/devocionais.php, admin/lider.php, admin/relatorios_gerais.php -->
<!-- 5-point checklist: (1) no PHP error/SQLSTATE/Fatal/Warning/Notice text; (2) no 375px horizontal scroll/overflow; (3) text legible light+dark; (4) buttons >=44px, radius 8-12px; (5) type matches canonical scale -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 environment + baseline confirmation</name>
  <read_first>
    - .planning/phases/10-harmonizacao-visual/10-VALIDATION.md (§Wave 0 Requirements, §Test Infrastructure)
    - .planning/phases/10-harmonizacao-visual/10-RESEARCH.md (§Environment Availability, §Validation Architecture Wave 0 Gaps)
  </read_first>
  <action>
    Confirm the validation environment is operational before the full sweep:
    - Start the local server: run `run_server.bat` (or `php -S localhost:8080 -t .`) and confirm `http://localhost:8080/applouvor/admin/index.php` responds. If PHP CLI is unavailable in shell, fall back to the production URL `https://vilela.eng.br/applouvor` and note the fallback in SUMMARY (less safe, but acceptable for read-only audit).
    - Confirm Playwright chromium is installed: `playwright install chromium` (idempotent — safe to re-run).
    - Confirm the runner script exists: `.agent/skills/webapp-testing/scripts/playwright_runner.py`. If `verify_all_pages.py` exists at repo root, prefer it; otherwise drive `playwright_runner.py` per route.
    - Confirm the runner can log in as admin AND as músico to reach role-gated routes (per VALIDATION Wave 0). Note credentials/session approach in SUMMARY.
  </action>
  <verify>
    <automated>cd "$PWD"; python .agent/skills/webapp-testing/scripts/playwright_runner.py http://localhost:8080/applouvor/admin/index.php --screenshot 2>&1 | head -5 || echo RUNNER_OR_SERVER_UNAVAILABLE_USE_FALLBACK</automated>
  </verify>
  <acceptance_criteria>
    - Local server (or production fallback) responds 200 on admin/index.php — recorded in SUMMARY
    - Playwright chromium present (install command exits cleanly)
    - Runner script path confirmed and able to authenticate as admin + músico (recorded)
    - Audit method chosen (aggregate verify_all_pages.py vs per-route loop) recorded in SUMMARY
  </acceptance_criteria>
  <done>Validation environment is confirmed operational (server up, Playwright ready, auth works) and the audit method is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Full 24-screenshot audit across all admin/ + app/ routes</name>
  <read_first>
    - .planning/phases/10-harmonizacao-visual/10-VALIDATION.md (§Full suite command, §5-Point Regression Checklist, §Per-Task Verification Map row 10-04)
    - .planning/phases/10-harmonizacao-visual/10-RESEARCH.md (§Critical Routes for Visual Audit, §Regression Check Protocol)
  </read_first>
  <action>
    Run the full audit. If `verify_all_pages.py` exists: `python verify_all_pages.py --screenshots --viewports 375,1280 --dark`. Otherwise loop `playwright_runner.py <route> --screenshot` over the 8 critical routes at 375px and 1280px, then toggle dark mode and re-shoot at 375px — producing the 24-screenshot set (8 routes x [375, 1280, dark@375]).
    Critical routes: admin/index.php, admin/escalas.php, admin/escala_detalhe.php, admin/repertorio.php, admin/oracao.php, admin/devocionais.php, admin/lider.php, admin/relatorios_gerais.php. ALSO sweep any app/ músico routes (app/index.php and músico escala view) for HTTP 200 + overflow.
    For EACH screenshot, apply the 5-point checklist and record pass/fail in a table in the SUMMARY:
    1. No PHP error/`SQLSTATE`/`Fatal error`/`Warning:`/`Notice:` text rendered.
    2. No horizontal scroll / element overflow at 375px.
    3. Text legible in light AND dark mode (no white-on-white / dark-on-dark).
    4. Buttons >= 44px touch height; radius 8-12px (DevTools/visual spot-check on escala_detalhe "Confirmar presença", oracao pray buttons, relatorios print button).
    5. Typography matches canonical scale (no heading oversized/undersized vs siblings; .date-day display, .pib-card-title 20px, KPI 30px).
    If ANY check fails on a route, identify the offending file/selector, apply the minimal token-aligned fix (NOT a literal — keep the harmonisation intent), re-run that route, and record the fix in SUMMARY. Re-run the affected wave's relevant grep gates if a CSS file is touched.
  </action>
  <verify>
    <automated>cd "$PWD"; python verify_all_pages.py 2>&1 | tail -20 || (for p in admin/index.php admin/escalas.php admin/escala_detalhe.php admin/repertorio.php admin/oracao.php admin/devocionais.php admin/lider.php admin/relatorios_gerais.php; do python .agent/skills/webapp-testing/scripts/playwright_runner.py http://localhost:8080/applouvor/$p --screenshot 2>&1 | grep -iE 'status|error|200|500'; done)</automated>
  </verify>
  <acceptance_criteria>
    - All 8 critical routes (+ app/ músico routes) return HTTP 200 with no `Fatal error`/`SQLSTATE`/`Warning:`/`Notice:` text — recorded as PASS in the SUMMARY route table
    - 24-screenshot set captured (8 routes x 375 + 1280 + dark@375) — file list referenced in SUMMARY
    - 5-point checklist recorded per route with explicit PASS/FAIL; zero unresolved FAIL rows
    - Any defect found is fixed with a token-aligned change and re-verified; the fix + re-run is documented
  </acceptance_criteria>
  <done>All routes pass the 5-point checklist across 375px/desktop/dark-mode with zero PHP errors and zero unresolved regressions; results tabulated in SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Diego harmony sign-off</name>
  <what-built>
    Waves 1-3 harmonised the app's typography (token-based scale), buttons (44px matrix + states), spacing/radius (tokens), and removed the 5 cascade-corrupting :root blocks. Task 2 produced a 24-screenshot audit set (8 routes x 375px + 1280px + dark-mode@375px) that passed the automated 5-point regression checklist.
  </what-built>
  <how-to-verify>
    1. Open the 24-screenshot set captured in Task 2 (or browse the live routes at http://localhost:8080/applouvor).
    2. View the 375px (mobile) and 1280px (desktop) sets side-by-side for: escalas, escala_detalhe, repertorio, oracao, devocionais, lider dashboard, admin dashboard, relatorios.
    3. Confirm subjective harmony per 10-VALIDATION.md Manual-Only table: sizes feel balanced, nothing too big or too squeezed; spiritual pages read green (not blue), blue stays the app accent.
    4. Toggle dark mode on oracao, devocionais, and the dashboard; confirm contrast feels intentional (cards dark, text legible).
  </how-to-verify>
  <resume-signal>Type "approved" if the visual harmony is acceptable, or describe the specific pages/elements that still feel off so they can be adjusted before closing the phase.</resume-signal>
</task>

</tasks>

<threat_model>
No new attack surface — this is a read-only audit wave with no code changes (any defect fix is CSS/token-only). No new input handling, auth, data flow, or external surface. No threats at ASVS L1. The non-security risk is shipping an undetected visual regression, which is precisely what this wave's full 24-screenshot 5-point audit exists to catch.
</threat_model>

<verification>
- All admin/ + app/ routes return HTTP 200 with no PHP error text
- 24-screenshot set passes the 5-point checklist (no overflow, dark-mode parity, 44px buttons, canonical type)
- Diego approves overall harmony
</verification>

<success_criteria>
Every admin and músico route is visually consistent and regression-free at 375px, desktop, and dark mode, with zero PHP errors, and Diego has signed off on the overall harmony. (UI-04)
</success_criteria>

<output>
After completion, create `.planning/phases/10-harmonizacao-visual/10-04-SUMMARY.md`
</output>
