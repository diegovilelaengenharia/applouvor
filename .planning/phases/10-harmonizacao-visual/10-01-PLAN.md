---
phase: 10-harmonizacao-visual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/css/pages/oracao.css
  - assets/css/pages/devocionais.css
  - assets/css/pages/leitura.css
  - assets/css/pages/evento-detalhe.css
  - assets/css/pages/avisos.css
autonomous: true
requirements: [UI-03]

must_haves:
  truths:
    - "No page CSS file declares a :root {} block (the design-system token cascade is clean)"
    - "Spiritual/green pages (oracao, devocionais) render their accent via --color-success / --reading-primary, not via a hijacked --primary"
    - "Pages loaded after the cleaned files (escalas, repertorio) keep the blue --primary in the same tab session"
    - "Dark mode on oracao/devocionais/leitura/evento-detalhe/avisos works because tokens resolve to design-system dark overrides"
  artifacts:
    - path: "assets/css/pages/oracao.css"
      provides: "Prayer page styles with no :root override"
      contains: "var(--color-success)"
    - path: "assets/css/pages/devocionais.css"
      provides: "Devotional page styles with no :root override"
      contains: "var(--reading-primary)"
    - path: "assets/css/pages/leitura.css"
      provides: "Reading page styles with no :root override"
    - path: "assets/css/pages/evento-detalhe.css"
      provides: "Event detail styles with no :root override"
    - path: "assets/css/pages/avisos.css"
      provides: "Notices page styles with no :root override"
      contains: "var(--teal-primary)"
  key_links:
    - from: "assets/css/pages/oracao.css"
      to: "design-system.css --color-success / --green-* tokens"
      via: "var() reference replacing former local --primary"
      pattern: "var\\(--color-success\\)|var\\(--green-"
    - from: "assets/css/pages/avisos.css"
      to: "design-system.css --teal-* tokens"
      via: "var() reference replacing local --teal redefinition"
      pattern: "var\\(--teal-(primary|bg|border)\\)"
---

<objective>
Remove the five page-level `:root {}` override blocks that silently mutate shared design-system tokens for the entire tab session. This is the cascade-safety prerequisite for all later waves: while these blocks exist, every downstream token swap is unstable because `--primary`, `--radius-lg`, `--shadow-sm`, etc. are corrupted globally.

Files: oracao.css, devocionais.css, leitura.css, evento-detalhe.css, avisos.css.

Purpose: Restore a single source of truth (design-system.css) for all CSS custom properties so blue stays blue, green pages use semantic green tokens, and dark mode works automatically.
Output: Five page CSS files with no `:root` block and every former-local `var()` reference repointed to the correct semantic design-system token.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-harmonizacao-visual/10-UI-SPEC.md
@.planning/phases/10-harmonizacao-visual/10-RESEARCH.md
@.planning/phases/10-harmonizacao-visual/10-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Canonical tokens already declared in design-system.css. Use these verbatim. -->
--color-success: var(--green-500);    /* #22c55e */
--green-600: #16a34a;                  /* hover/dark green */
--green-100: #dcfce7;                  /* light green */
--green-50:  #f0fdf4;                  /* subtle green */
--reading-primary: var(--green-600);   /* spiritual/reading accent */
--teal-primary: var(--teal-500);
--teal-bg: var(--teal-50);
--teal-border: var(--teal-200);
--color-primary: var(--blue-500);      /* #3b82f6 — must NEVER be redefined to green */
--radius-lg: 12px;                      /* NOTE: oracao :root wrongly set this to 16px */
--bg-surface, --border-color, --text-main, --text-muted, --shadow-sm, --shadow-md  /* all already in design-system */
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kill :root in oracao.css and devocionais.css, repoint green references</name>
  <read_first>
    - assets/css/pages/oracao.css (full file — locate :root block lines 4–21 and every var(--primary*) usage)
    - assets/css/pages/devocionais.css (full file — locate :root block lines 4–21 and every var(--primary*) usage)
    - assets/css/design-system.css (token source of truth — confirm --color-success, --green-600, --green-100, --green-50, --reading-primary names/values)
    - .planning/phases/10-harmonizacao-visual/10-UI-SPEC.md (§Color "Semantic reuse rule" + §Redundant Declarations Audit critical table)
  </read_first>
  <action>
    In `assets/css/pages/oracao.css`: DELETE the entire `:root { ... }` block (lines ~4–21) which redefines `--primary: #22c55e`, `--primary-hover`, `--primary-light`, `--primary-subtle`, `--bg-surface`, `--border-color`, `--text-main`, `--text-muted`, `--radius-lg: 16px`, `--shadow-sm`. BEFORE deleting, grep the file for every `var(--primary*)` usage and replace each, in place, with the correct semantic token:
    - `var(--primary)` → `var(--color-success)`  /* green-500 */
    - `var(--primary-hover)` → `var(--green-600)`
    - `var(--primary-light)` → `var(--green-100)`
    - `var(--primary-subtle)` → `var(--green-50)`
    - `var(--bg-surface)`, `var(--border-color)`, `var(--text-main)`, `var(--text-muted)`, `var(--shadow-sm)` → leave as-is (they now resolve to design-system, which is correct; do NOT rename).
    - `var(--radius-lg)` → leave the token reference, but be aware corners drop from 16px to canonical 12px (intentional per UI-SPEC — not a bug).
    Also scan oracao.css for any raw green hex literal `#22c55e` and replace with `var(--color-success)` (UI-SPEC §Redundant Declarations / Hard-coded colour hex). Do NOT touch `.fab-create` here (handled in Wave 3).
    In `assets/css/pages/devocionais.css`: same procedure. The devocionais `:root` sets `--primary: #16A34A` (green-600). Replace `var(--primary)` → `var(--reading-primary)` (which IS green-600, the correct spiritual accent per UI-SPEC), `var(--primary-hover)` → `var(--green-600)`, `var(--primary-light)` → `var(--green-100)`, `var(--primary-subtle)` → `var(--green-50)`. Delete the `:root` block after repointing.
    Per pitfall #6: after removing each `:root`, scan for raw `background: #ffffff` / `background: white` on card elements and replace with `var(--bg-surface)` so dark mode works.
  </action>
  <verify>
    <automated>cd "$PWD"; grep -c ':root' assets/css/pages/oracao.css; grep -c ':root' assets/css/pages/devocionais.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ':root' assets/css/pages/oracao.css` returns 0 (no :root block remains)
    - `grep -c ':root' assets/css/pages/devocionais.css` returns 0
    - `grep -c 'var(--primary' assets/css/pages/oracao.css` returns 0 (all primary refs repointed)
    - `grep -c 'var(--primary' assets/css/pages/devocionais.css` returns 0
    - oracao.css contains at least one `var(--color-success)` reference (grep finds it)
    - devocionais.css contains at least one `var(--reading-primary)` reference
    - oracao.css contains no raw `#22c55e` literal (grep -F '#22c55e' returns nothing)
  </acceptance_criteria>
  <done>Both files have no :root block, zero var(--primary*) references, and use semantic green tokens. Blue --primary is no longer hijacked for later pages.</done>
</task>

<task type="auto">
  <name>Task 2: Kill :root in leitura.css, evento-detalhe.css, avisos.css</name>
  <read_first>
    - assets/css/pages/leitura.css (full file — :root block lines 6–10)
    - assets/css/pages/evento-detalhe.css (full file — :root block lines 4–27, 13 tokens)
    - assets/css/pages/avisos.css (full file — :root block lines 2–7, teal tokens)
    - assets/css/design-system.css (confirm --reading-primary, --teal-primary/--teal-bg/--teal-border, and the 13 tokens evento-detalhe redefines all already exist)
    - .planning/phases/10-harmonizacao-visual/10-RESEARCH.md (§Concrete Conflict Inventory — Verified :root Override Files table)
  </read_first>
  <action>
    `assets/css/pages/leitura.css`: DELETE the `:root { --reading-primary: #10b981; ... }` block (lines ~6–10). design-system.css already declares `--reading-primary: var(--green-600)`. After deletion, the page's `var(--reading-primary)` usages resolve to design-system automatically — no further replacement needed. Verify no other token was redefined in that block; if it was, repoint to its design-system equivalent.
    `assets/css/pages/evento-detalhe.css`: DELETE the entire `:root { ... }` block (lines ~4–27) which redeclares 13 tokens (`--primary`, `--bg-body`, `--bg-surface`, `--text-main`, `--text-muted`, `--border-color`, `--border-subtle`, `--shadow-sm`, `--shadow-md`, `--radius-lg`, `--blue-500`, `--green-500`, `--red-500`, `--yellow-500`) — all already in design-system with equal-or-better values. These names match design-system exactly, so existing `var()` usages in the file keep working after deletion. This file is imported globally via app-main.css (RESEARCH Open Question #2) — its blast radius is the whole app, so removing it is the single highest-impact fix in this wave. Replace any raw hex that was a duplicate of these tokens with the token reference.
    `assets/css/pages/avisos.css`: DELETE the `:root { --teal-primary; --teal-light; --teal-bg; --teal-border; }` block (lines ~2–7). These local values DIFFER from design-system, so after removal the page will use design-system's `--teal-primary` (var(--teal-500)), `--teal-bg` (var(--teal-50)), `--teal-border` (var(--teal-200)). Existing `var(--teal-*)` usages now resolve to design-system — confirm they read sensibly; `--teal-light` is not a design-system token, so replace any `var(--teal-light)` usage with `var(--teal-bg)` or `var(--teal-200)` as visually appropriate.
  </action>
  <verify>
    <automated>cd "$PWD"; grep -c ':root' assets/css/pages/leitura.css; grep -c ':root' assets/css/pages/evento-detalhe.css; grep -c ':root' assets/css/pages/avisos.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ':root' assets/css/pages/leitura.css` returns 0
    - `grep -c ':root' assets/css/pages/evento-detalhe.css` returns 0
    - `grep -c ':root' assets/css/pages/avisos.css` returns 0
    - avisos.css contains no `var(--teal-light)` reference (grep returns nothing — replaced)
    - avisos.css references `var(--teal-primary)` and/or `var(--teal-bg)` (grep finds at least one)
    - evento-detalhe.css does NOT redeclare `--primary` (grep -c -- '--primary:' returns 0)
  </acceptance_criteria>
  <done>All three files have no :root block. Reading uses design-system --reading-primary; notices use design-system --teal-* tokens; event detail no longer pollutes the global token set for the whole app.</done>
</task>

<task type="auto">
  <name>Task 3: Regression sweep — confirm no page corrupted by :root removal</name>
  <read_first>
    - .planning/phases/10-harmonizacao-visual/10-VALIDATION.md (§Sampling Rate, §5-Point Regression Checklist, §Critical Routes)
    - .planning/phases/10-harmonizacao-visual/10-RESEARCH.md (§Common Pitfalls 1 and 6 — surviving var() refs + dark-mode breakage)
  </read_first>
  <action>
    Start the local server if not already running (`run_server.bat`, serving `localhost:8080`). Run the route audit against the five touched pages PLUS two pages that load AFTER evento-detalhe in the cascade (escalas, repertorio) to confirm no green/blue bleed:
    `python verify_all_pages.py` (HTTP 200 + PHP error scan across all routes), or if that script is absent, loop the Playwright runner per route:
    `python .agent/skills/webapp-testing/scripts/playwright_runner.py http://localhost:8080/applouvor/admin/oracao.php --screenshot`
    Repeat for devocionais.php, leitura.php, evento_detalhe.php (or the route that loads avisos), escalas.php, repertorio.php.
    Apply the 5-point regression checklist to each screenshot. Specifically verify: (a) oracao/devocionais green accents are GREEN not blue and not hijacking the rest; (b) escalas/repertorio --primary is still BLUE; (c) dark-mode on oracao shows dark card backgrounds (--slate-800), not stuck white. If any green element turned blue → a surviving `var(--primary)` was missed in Task 1; fix it.
    If PHP CLI is unavailable in shell, fall back to manual browser screenshots per VALIDATION.md note; record which method was used in the SUMMARY.
  </action>
  <verify>
    <automated>cd "$PWD"; grep -rc ':root' assets/css/pages/oracao.css assets/css/pages/devocionais.css assets/css/pages/leitura.css assets/css/pages/evento-detalhe.css assets/css/pages/avisos.css</automated>
  </verify>
  <acceptance_criteria>
    - All five files report `:root` count of 0 (combined grep shows 0 for each)
    - Route audit: each of oracao.php, devocionais.php, leitura.php, evento_detalhe.php, escalas.php, repertorio.php returns HTTP 200 with no `Fatal error` / `SQLSTATE` / `Warning:` / `Notice:` text in the response body
    - Manual confirmation recorded in SUMMARY: green pages render green, blue pages render blue, dark mode cards are dark — no cascade bleed
  </acceptance_criteria>
  <done>All five :root blocks removed; the six audited routes return clean HTTP 200 with no PHP errors; visual sweep confirms no color cascade bleed across the tab session.</done>
</task>

</tasks>

<threat_model>
No new attack surface — CSS/presentation-only changes; no threats at ASVS L1. No new input handling, auth, data flow, or external surface is introduced. The only non-security risk is visual regression / broken layout from removing the :root blocks, mitigated by the Task 3 regression sweep (route audit + 375px/desktop/dark-mode screenshots) defined in 10-VALIDATION.md.
</threat_model>

<verification>
- `grep -c ':root'` returns 0 for all five files
- No `var(--primary*)` references remain in oracao.css or devocionais.css
- Six audited routes return HTTP 200 with no PHP error text
- Visual: green pages green, blue pages blue, dark mode dark — no bleed
</verification>

<success_criteria>
The design-system token cascade is clean: zero page-level `:root` blocks. Spiritual pages use semantic green/reading tokens; notices use teal tokens; all later-loaded pages retain the canonical blue `--primary`. Dark mode works on all five pages via design-system token resolution. (UI-03)
</success_criteria>

<output>
After completion, create `.planning/phases/10-harmonizacao-visual/10-01-SUMMARY.md`
</output>
