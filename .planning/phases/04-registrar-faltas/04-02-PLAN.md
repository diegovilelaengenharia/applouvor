---
phase: 04-registrar-faltas
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - api/save_absences.php
autonomous: true
requirements:
  - FAL-03

must_haves:
  truths:
    - "api/save_absences.php existe e responde com JSON"
    - "Endpoint valida que o usuário logado é admin (retorna 403 se não for)"
    - "Endpoint valida que schedule_id corresponde a uma escala do passado"
    - "Endpoint aceita array participants[] com {user_id, status, note}"
    - "Status válidos aceitos: confirmed, declined, pending, absent, absent_justified"
    - "Atualiza schedule_users.status e schedule_users.absence_note para cada participante"
    - "Valida que cada user_id pertence ao schedule_id antes de atualizar"
    - "Retorna json com success:true e count de registros atualizados"
  artifacts:
    - path: "api/save_absences.php"
      provides: "API de salvamento de presenças/faltas pós-culto"
      contains: "save_absences"
  key_links:
    - from: "api/save_absences.php"
      to: "schedule_users (table)"
      via: "PDO UPDATE"
      pattern: "schedule_users"
---

<objective>
Criar o endpoint `api/save_absences.php` que recebe o schedule_id e o array de participantes com seus status de presença, valida os dados e persiste no banco.

Purpose: Sem esse endpoint, o formulário de registro de faltas (Plan 04-01) não tem onde salvar os dados.
Output: `api/save_absences.php` funcional com validações de segurança completas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-registrar-faltas/04-CONTEXT.md

<interfaces>
De includes/auth.php:
- `checkLogin()` — redireciona para login se não autenticado (ou usar $_SESSION diretamente em APIs)
- `$_SESSION['user_id']` — ID do usuário logado
- `$_SESSION['user_role']` — 'admin' ou 'user'

De includes/db.php:
- Expõe `$pdo` (PDO)

Padrão de API (ver api/confirm_scale.php e api/roteiro.php):
- `header('Content-Type: application/json');` no topo
- `require_once '../includes/auth.php';` (não checkAdmin() — auth.php em APIs usa $_SESSION direto)
- Resposta: `json_encode(['success' => bool, 'message' => '...'])`
- Validar sessão manualmente: `$userId = $_SESSION['user_id'] ?? 0; if (!$userId) { http_response_code(401); ... exit; }`

Banco relevante após migration 004:
- `schedule_users(schedule_id, user_id, status ENUM(...,'absent','absent_justified'), absence_note TEXT NULL)`
- `schedules(id, event_date)` — verificar que event_date < CURDATE()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar api/save_absences.php</name>
  <files>api/save_absences.php</files>

  <read_first>
    - api/confirm_scale.php — padrão completo de API simples (auth, JSON input, PDO update)
    - api/roteiro.php — padrão de API com validações mais complexas (admin check, validação de schedule)
  </read_first>

  <action>
Criar `api/save_absences.php` com o seguinte conteúdo:

```php
<?php
// api/save_absences.php — Persiste status de presença/falta pós-culto (admin only)
header('Content-Type: application/json');
require_once '../includes/auth.php';
require_once '../includes/db.php';

$userId   = $_SESSION['user_id']   ?? 0;
$userRole = $_SESSION['user_role'] ?? '';

if (!$userId) {
    http_response_code(401);
    echo json_encode(['success' => false, 'message' => 'Não autenticado']);
    exit;
}

if ($userRole !== 'admin') {
    http_response_code(403);
    echo json_encode(['success' => false, 'message' => 'Apenas administradores podem registrar faltas']);
    exit;
}

if ($_SERVER['REQUEST_METHOD'] !== 'POST') {
    http_response_code(405);
    echo json_encode(['success' => false, 'message' => 'Método não permitido']);
    exit;
}

$data = json_decode(file_get_contents('php://input'), true);
$scheduleId   = (int)($data['schedule_id'] ?? 0);
$participants = $data['participants'] ?? [];

if (!$scheduleId || empty($participants) || !is_array($participants)) {
    echo json_encode(['success' => false, 'message' => 'Dados inválidos: schedule_id e participants[] obrigatórios']);
    exit;
}

// Validar que a escala existe e é do passado
$stmtSched = $pdo->prepare("SELECT id, event_date FROM schedules WHERE id = ?");
$stmtSched->execute([$scheduleId]);
$schedule = $stmtSched->fetch(PDO::FETCH_ASSOC);

if (!$schedule) {
    echo json_encode(['success' => false, 'message' => 'Escala não encontrada']);
    exit;
}

if ($schedule['event_date'] >= date('Y-m-d')) {
    echo json_encode(['success' => false, 'message' => 'Só é possível registrar faltas em escalas passadas']);
    exit;
}

// Buscar user_ids válidos para esta escala (guard de integridade)
$stmtValidUsers = $pdo->prepare("SELECT user_id FROM schedule_users WHERE schedule_id = ?");
$stmtValidUsers->execute([$scheduleId]);
$validUserIds = array_column($stmtValidUsers->fetchAll(PDO::FETCH_ASSOC), 'user_id');

$validStatuses = ['confirmed', 'declined', 'pending', 'absent', 'absent_justified'];

$stmtUpdate = $pdo->prepare("
    UPDATE schedule_users
    SET status = ?, absence_note = ?
    WHERE schedule_id = ? AND user_id = ?
");

$updatedCount = 0;
$errors = [];

foreach ($participants as $p) {
    $pUserId = (int)($p['user_id'] ?? 0);
    $pStatus = $p['status'] ?? 'confirmed';
    $pNote   = isset($p['note']) && $p['note'] !== '' ? trim(substr($p['note'], 0, 500)) : null;

    // Validar que o user_id pertence a esta escala
    if (!in_array($pUserId, $validUserIds)) {
        $errors[] = "user_id $pUserId não pertence a esta escala";
        continue;
    }

    // Validar status contra whitelist
    if (!in_array($pStatus, $validStatuses)) {
        $pStatus = 'confirmed'; // fallback seguro
    }

    // Limpar nota se não for ausência
    if (!in_array($pStatus, ['absent', 'absent_justified'])) {
        $pNote = null;
    }

    try {
        $stmtUpdate->execute([$pStatus, $pNote, $scheduleId, $pUserId]);
        $updatedCount++;
    } catch (Exception $e) {
        $errors[] = "Erro ao atualizar user_id $pUserId";
        error_log("save_absences error for user $pUserId: " . $e->getMessage());
    }
}

if ($updatedCount === 0 && !empty($errors)) {
    echo json_encode(['success' => false, 'message' => 'Nenhum registro atualizado', 'errors' => $errors]);
    exit;
}

echo json_encode([
    'success' => true,
    'message' => "$updatedCount participante(s) atualizados",
    'updated' => $updatedCount,
]);
```
  </action>

  <verify>
    <automated>php -l api/save_absences.php</automated>
  </verify>

  <acceptance_criteria>
    - `php -l api/save_absences.php` retorna "No syntax errors detected"
    - Contém verificação de sessão com retorno 401 se não autenticado
    - Contém verificação `$userRole !== 'admin'` com retorno 403
    - Valida que `event_date < date('Y-m-d')` — retorna erro se escala futura
    - Valida que cada user_id pertence ao schedule (via $validUserIds)
    - Status validado contra whitelist de 5 valores
    - `absence_note` é limpa (null) para status != absent/absent_justified
    - `absence_note` truncada a 500 chars para evitar abuso
    - Retorna `success: true` com count de atualizados
    - Não expõe mensagens de erro PDO (apenas "Erro ao atualizar...")
  </acceptance_criteria>

  <done>
API api/save_absences.php criada com validações completas: auth, admin-only, escala passada, user_ids válidos, status whitelist.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → api/save_absences.php | Admin envia schedule_id + participants[] via POST JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04B-01 | Spoofing | api/save_absences.php | mitigate | Verifica `$_SESSION['user_id']` — 401 se não autenticado |
| T-04B-02 | Elevation | admin check | mitigate | `$userRole !== 'admin'` — 403 para usuários comuns |
| T-04B-03 | Tampering | user_id não pertence à escala | mitigate | Guard: `in_array($pUserId, $validUserIds)` antes de UPDATE |
| T-04B-04 | Tampering | escala futura | mitigate | `event_date >= date('Y-m-d')` — retorna erro; só escalas passadas |
| T-04B-05 | Tampering | status inválido | mitigate | Whitelist de 5 valores — fallback para 'confirmed' se inválido |
| T-04B-06 | Injection | absence_note | mitigate | PDO prepared statements — sem concatenação; truncado a 500 chars |
</threat_model>

<verification>
1. `php -l api/save_absences.php` — sem erros de sintaxe
2. `grep "admin" api/save_absences.php` — verificação de admin presente
3. `grep "event_date" api/save_absences.php` — guard de escala passada presente
4. `grep "validUserIds\|validStatuses" api/save_absences.php` — ambas as validações presentes
5. `grep "prepare\|execute" api/save_absences.php` — uso de prepared statements
</verification>

<success_criteria>
- api/save_absences.php sem erros de sintaxe PHP
- Admin-only (403 para não-admin)
- Só processa escalas passadas
- Valida user_ids contra participantes reais da escala
- Whitelist de status (5 valores válidos)
- absence_note limpa para status não-ausência
- Retorna contagem de registros atualizados
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-registrar-faltas/04-02-SUMMARY.md`
</output>
