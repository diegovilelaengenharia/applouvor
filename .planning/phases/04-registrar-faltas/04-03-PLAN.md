---
phase: 04-registrar-faltas
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - admin/membro_detalhe.php
autonomous: true
requirements:
  - FAL-04

must_haves:
  truths:
    - "admin/membro_detalhe.php inclui su.status e su.absence_note na query de histórico de escalas"
    - "Histórico exibe badge de status por escala: ✅ Presente, ❌ Faltou, ⚠️ Justificou, 🕐 Pendente"
    - "Stats de presença exibem breakdown: total de escalas, presentes, faltas, justificadas"
    - "Taxa de presença calculada corretamente: (confirmed + pending) / total * 100"
    - "absence_note exibida quando status é absent ou absent_justified (tooltip ou texto abaixo)"
  artifacts:
    - path: "admin/membro_detalhe.php"
      provides: "Perfil do membro com histórico de presença integrado"
      contains: "absent_justified"
  key_links:
    - from: "admin/membro_detalhe.php"
      to: "schedule_users (table)"
      via: "PDO query com JOIN"
      pattern: "schedule_users"
---

<objective>
Integrar os dados de presença/falta em `admin/membro_detalhe.php`: atualizar a query do histórico de escalas para incluir `status` e `absence_note`, exibir badges visuais de presença em cada escala do histórico, e mostrar estatísticas de presença com breakdown.

Purpose: FAL-04 — o histórico do membro deve refletir as faltas registradas. Sem isso, o esforço de registro de faltas dos Plans 04-01 e 04-02 não tem valor pastoral visível.
Output: `admin/membro_detalhe.php` com histórico de presenças e stats de breakdown.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-registrar-faltas/04-CONTEXT.md

<interfaces>
Banco relevante:
- `schedule_users(schedule_id, user_id, status, absence_note)` — status inclui agora absent/absent_justified
- `schedules(id, event_type, event_date, event_time, notes)`

Estado atual de admin/membro_detalhe.php:
- Query `$stmtHistory` em linha ~38: busca schedules JOIN schedule_users — NÃO inclui status nem absence_note
- Query `$stmtStats` em linha ~48: busca total_escalas, primeira_escala, ultima_escala — sem breakdown de status
- Exibe lista de escalas sem status de presença
- Exibe stats de frequência média (em dias) — manter, mas adicionar stats de presença

O que adicionar:
1. Incluir `su.status` e `su.absence_note` em $stmtHistory
2. Calcular stats de presença: confirmed+pending = presente; absent = faltou; absent_justified = justificou
3. Exibir badge por escala no loop de $schedules
4. Exibir breakdown de stats
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar query de histórico e stats em admin/membro_detalhe.php</name>
  <files>admin/membro_detalhe.php</files>

  <read_first>
    - admin/membro_detalhe.php — ler o arquivo completo para entender:
      1. A query $stmtHistory (busca histórico de escalas) — localizar para adicionar su.status e su.absence_note
      2. A query $stmtStats (busca estatísticas) — localizar para adicionar breakdown de status
      3. O HTML de exibição do histórico — localizar onde adicionar os badges de status
      4. A seção de estatísticas — localizar onde adicionar o breakdown
  </read_first>

  <action>
Fazer as seguintes alterações em `admin/membro_detalhe.php`:

### Alteração 1: Query $stmtHistory — adicionar status e absence_note

Localizar a query que busca o histórico de escalas (começa com SELECT s.*, ...). Modificar para incluir `su.status` e `su.absence_note`:

```php
$stmtHistory = $pdo->prepare("
    SELECT s.*, 
           (SELECT COUNT(*) FROM schedule_songs WHERE schedule_id = s.id) as total_songs,
           su.status as presence_status,
           su.absence_note
    FROM schedules s
    JOIN schedule_users su ON s.id = su.schedule_id
    WHERE su.user_id = ?
    ORDER BY s.event_date DESC
");
```

### Alteração 2: Calcular stats de presença após as queries existentes

Após o código de cálculo de frequência média, adicionar o cálculo de breakdown de presença:

```php
// Calcular breakdown de presença
$totalEscalas = count($schedules);
$totalPresente = 0;
$totalFaltou = 0;
$totalJustificou = 0;
$totalPendente = 0;

foreach ($schedules as $sc) {
    $st = $sc['presence_status'] ?? 'pending';
    if (in_array($st, ['confirmed', 'pending'])) $totalPresente++;
    elseif ($st === 'absent') $totalFaltou++;
    elseif ($st === 'absent_justified') $totalJustificou++;
    else $totalPendente++;
}

$taxaPresenca = $totalEscalas > 0
    ? round(($totalPresente / $totalEscalas) * 100)
    : 0;
```

### Alteração 3: Adicionar bloco de stats de presença no HTML

Localizar a seção de estatísticas no HTML (onde aparece $stats['total_escalas'] e a frequência média).
Adicionar após as stats existentes um bloco de breakdown de presença:

```php
<!-- Stats de Presença -->
<div class="presence-stats-card pib-card" style="margin-top:12px;padding:16px;">
  <h4 style="margin:0 0 12px;font-size:0.9rem;font-weight:700;color:var(--gray-700,#374151);">
    Histórico de Presença
  </h4>
  <div style="display:flex;gap:10px;flex-wrap:wrap;">
    <div style="flex:1;min-width:80px;text-align:center;padding:10px;background:#d1fae5;border-radius:8px;">
      <div style="font-size:1.4rem;font-weight:800;color:#065f46;"><?= $totalPresente ?></div>
      <div style="font-size:0.7rem;font-weight:600;color:#047857;">Presente</div>
    </div>
    <div style="flex:1;min-width:80px;text-align:center;padding:10px;background:#fee2e2;border-radius:8px;">
      <div style="font-size:1.4rem;font-weight:800;color:#991b1b;"><?= $totalFaltou ?></div>
      <div style="font-size:0.7rem;font-weight:600;color:#b91c1c;">Faltou</div>
    </div>
    <div style="flex:1;min-width:80px;text-align:center;padding:10px;background:#fef3c7;border-radius:8px;">
      <div style="font-size:1.4rem;font-weight:800;color:#92400e;"><?= $totalJustificou ?></div>
      <div style="font-size:0.7rem;font-weight:600;color:#b45309;">Justificou</div>
    </div>
    <div style="flex:1;min-width:80px;text-align:center;padding:10px;background:var(--blue-50,#eff6ff);border-radius:8px;border:2px solid var(--blue-500,#3b82f6);">
      <div style="font-size:1.4rem;font-weight:800;color:var(--blue-700,#1d4ed8);"><?= $taxaPresenca ?>%</div>
      <div style="font-size:0.7rem;font-weight:600;color:var(--blue-600,#2563eb);">Taxa</div>
    </div>
  </div>
</div>
```

### Alteração 4: Adicionar badge de status em cada item do histórico de escalas

Localizar o loop `foreach ($schedules as $schedule)` no HTML e dentro do card de cada escala adicionar o badge de status:

```php
<?php
$pStatus = $schedule['presence_status'] ?? 'pending';
$statusLabel = match($pStatus) {
    'confirmed'         => ['✅', 'Presente',   '#d1fae5', '#065f46'],
    'absent'            => ['❌', 'Faltou',      '#fee2e2', '#991b1b'],
    'absent_justified'  => ['⚠️', 'Justificou',  '#fef3c7', '#92400e'],
    'declined'          => ['🚫', 'Recusou',     '#f3f4f6', '#6b7280'],
    default             => ['🕐', 'Pendente',    '#f3f4f6', '#6b7280'],
};
[$icon, $label, $bg, $color] = $statusLabel;
?>
<span style="display:inline-flex;align-items:center;gap:4px;font-size:0.7rem;font-weight:700;
             padding:3px 8px;border-radius:12px;background:<?= $bg ?>;color:<?= $color ?>;">
  <?= $icon ?> <?= $label ?>
</span>
<?php if (in_array($pStatus, ['absent','absent_justified']) && !empty($schedule['absence_note'])): ?>
<p style="font-size:0.72rem;color:var(--gray-500,#6b7280);margin:4px 0 0;font-style:italic;">
  <?= htmlspecialchars($schedule['absence_note']) ?>
</p>
<?php endif; ?>
```

Integrar esse trecho dentro do card de escala do loop, após o título/tipo da escala.
  </action>

  <verify>
    <automated>php -l admin/membro_detalhe.php</automated>
  </verify>

  <acceptance_criteria>
    - `php -l admin/membro_detalhe.php` retorna "No syntax errors detected"
    - Query $stmtHistory inclui `su.status as presence_status` e `su.absence_note`
    - Variáveis `$totalPresente`, `$totalFaltou`, `$totalJustificou`, `$taxaPresenca` calculadas
    - Bloco de stats de presença exibe 4 cards: Presente (verde), Faltou (vermelho), Justificou (âmbar), Taxa (azul)
    - Loop de histórico exibe badge de status colorido por escala
    - absence_note exibida quando status é absent ou absent_justified
    - `htmlspecialchars()` usado em absence_note exibida no HTML
    - Cores de status consistentes com registrar_faltas.php (verde/vermelho/âmbar)
  </acceptance_criteria>

  <done>
admin/membro_detalhe.php atualizado com su.status+absence_note na query, stats de presença com breakdown e badges de status no histórico de escalas.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → admin/membro_detalhe.php (GET) | Usuário autenticado visualiza perfil do membro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04C-01 | XSS | absence_note no HTML | mitigate | `htmlspecialchars()` ao exibir absence_note no histórico |
| T-04C-02 | Information Disclosure | absence_note visível | accept | Página já é restrita a admin (checkAdmin existe) — nota interna não exposta ao músico |
</threat_model>

<verification>
1. `php -l admin/membro_detalhe.php` — sem erros de sintaxe
2. `grep "presence_status\|absence_note" admin/membro_detalhe.php` — ambos presentes (query e exibição)
3. `grep "taxaPresenca\|totalFaltou\|totalJustificou" admin/membro_detalhe.php` — variáveis calculadas
4. `grep "htmlspecialchars.*absence_note" admin/membro_detalhe.php` — proteção XSS presente
</verification>

<success_criteria>
- admin/membro_detalhe.php sem erros de sintaxe PHP
- Query de histórico inclui presence_status e absence_note
- Stats de presença: Presente, Faltou, Justificou, Taxa em cards coloridos
- Badge de status por escala no histórico (verde/vermelho/âmbar/cinza)
- absence_note exibida com htmlspecialchars quando aplicável
- Experiência visual consistente com a tela de registro de faltas (mesmas cores)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-registrar-faltas/04-03-SUMMARY.md`
</output>
