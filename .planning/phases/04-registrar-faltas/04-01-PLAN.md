---
phase: 04-registrar-faltas
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/004_schedule_users_absences.sql
  - admin/escalas.php
  - admin/registrar_faltas.php
autonomous: true
requirements:
  - FAL-01
  - FAL-02

must_haves:
  truths:
    - "Migration 004_schedule_users_absences.sql existe e altera schedule_users adicionando absent e absent_justified ao ENUM de status"
    - "Migration adiciona coluna absence_note TEXT NULL à tabela schedule_users"
    - "admin/escalas.php exibe botão/link 'Registrar Faltas' nos cards de escalas passadas, visível SOMENTE para admin"
    - "admin/registrar_faltas.php exibe lista dos participantes da escala com nome, instrumento e toggle de 3 estados"
    - "Toggle tem 3 estados: presente (padrão), faltou (absent), justificou (absent_justified)"
    - "Cada participante tem campo opcional de nota de ausência (input text)"
    - "Formulário envia para api/save_absences.php via POST JSON"
    - "Página valida que a escala é do passado antes de exibir o formulário"
    - "Acesso restrito a admin (checkAdmin())"
  artifacts:
    - path: "database/migrations/004_schedule_users_absences.sql"
      provides: "Migration SQL para ampliar schedule_users com estados de ausência"
      contains: "ALTER TABLE schedule_users"
    - path: "admin/registrar_faltas.php"
      provides: "Tela de registro de faltas para o líder"
      contains: "registrar_faltas"
  key_links:
    - from: "admin/registrar_faltas.php"
      to: "api/save_absences.php"
      via: "fetch() POST JSON"
      pattern: "save_absences"
    - from: "admin/escalas.php"
      to: "admin/registrar_faltas.php"
      via: "link com ?id=schedule_id"
      pattern: "registrar_faltas"
---

<objective>
Criar a migration de banco de dados para suportar `absent`/`absent_justified` na coluna `status` de `schedule_users`, adicionar o botão "Registrar Faltas" nas escalas passadas em `admin/escalas.php`, e criar a tela `admin/registrar_faltas.php` com lista de participantes e toggles de 3 estados.

Purpose: Base de UI para o fluxo de registro de faltas. Sem essa tela, o líder não tem como registrar presenças e ausências pós-culto.
Output: `database/migrations/004_schedule_users_absences.sql` + botão em escalas.php + `admin/registrar_faltas.php` funcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-registrar-faltas/04-CONTEXT.md

<interfaces>
De includes/db.php:
- Expõe `$pdo` como instância PDO já conectada ao banco
- Requer: `require_once '../includes/db.php';`

De includes/auth.php:
- `checkLogin()` — redireciona para login se não autenticado
- `checkAdmin()` — redireciona se não for admin
- `$_SESSION['user_id']` — ID do usuário logado
- `$_SESSION['user_role']` — 'admin' ou 'user'

De includes/layout.php:
- `renderAppHeader($title, $backUrl)` — cabeçalho padrão do app

Banco existente relevante:
- `schedule_users(schedule_id, user_id, status, instrument, is_rehearsed)` — status é ENUM
- `schedules(id, event_type, event_date, event_time, notes)` — tabela pai
- `users(id, name, instrument, avatar_color)` — perfil do membro

Padrão de admin pages (ver admin/escala_detalhe.php):
- `require_once '../includes/db.php';` + `require_once '../includes/layout.php';`
- `renderAppHeader('Título', 'url_voltar.php');`
- CSS inline ou `<link rel="stylesheet" href="../assets/css/pages/nome.css">`
- Cards com classe `.pib-card`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration SQL — adicionar absent/absent_justified ao ENUM + absence_note</name>
  <files>database/migrations/004_schedule_users_absences.sql</files>

  <read_first>
    - database/migrations/003_schedule_roteiro.sql — ver padrão de migration (comentário descritivo no topo)
    - database/migrations/add_dashboard_settings.sql — ver padrão ALTER TABLE no projeto
  </read_first>

  <action>
Criar `database/migrations/004_schedule_users_absences.sql`:

```sql
-- Migration 004: Ampliar schedule_users para suportar registro de faltas
-- Adiciona 'absent' (faltou sem aviso) e 'absent_justified' (justificou com antecedência)
-- ao ENUM de status. Peso pastoral distinto: absent_justified não conta como abandono.
-- absence_note: nota interna opcional do líder sobre o motivo da ausência.

ALTER TABLE schedule_users
  MODIFY COLUMN status ENUM('pending','confirmed','declined','absent','absent_justified')
    NOT NULL DEFAULT 'pending';

ALTER TABLE schedule_users
  ADD COLUMN absence_note TEXT NULL
    COMMENT 'Nota interna do líder sobre a ausência. Visível apenas para admin.';
  </action>

  <verify>
    <automated>php -r "echo file_exists('database/migrations/004_schedule_users_absences.sql') ? 'OK' : 'MISSING';"</automated>
  </verify>

  <acceptance_criteria>
    - Arquivo `database/migrations/004_schedule_users_absences.sql` existe
    - Contém `ALTER TABLE schedule_users`
    - Contém `absent` e `absent_justified` no ENUM
    - Contém `absence_note TEXT NULL`
    - Comentário explica a diferença pastoral entre absent e absent_justified
  </acceptance_criteria>

  <done>Migration 004 criada em database/migrations/004_schedule_users_absences.sql.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar botão "Registrar Faltas" nos cards de escalas passadas em admin/escalas.php</name>
  <files>admin/escalas.php</files>

  <read_first>
    - admin/escalas.php — ler o arquivo completo para entender onde os cards de escalas passadas são renderizados (procurar por $pastSchedules e os cards HTML)
  </read_first>

  <action>
Localizar no arquivo `admin/escalas.php` o loop que renderiza os cards de escalas passadas (`$pastSchedules`).

Dentro do card de cada escala passada, adicionar um link "Registrar Faltas" visível apenas para admin:

```php
<?php if ($_SESSION['user_role'] === 'admin'): ?>
<a href="registrar_faltas.php?id=<?= $s['id'] ?>"
   class="btn-registrar-faltas"
   style="display:inline-flex;align-items:center;gap:6px;font-size:0.75rem;font-weight:600;
          color:var(--orange-500,#f97316);text-decoration:none;padding:6px 10px;
          border:1px solid var(--orange-500,#f97316);border-radius:8px;margin-top:8px;">
  <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24"
       fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"
       stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
    <circle cx="9" cy="7" r="4"/><line x1="17" y1="8" x2="23" y2="8"/>
    <line x1="20" y1="5" x2="20" y2="11"/></svg>
  Registrar Faltas
</a>
<?php endif; ?>
```

Inserir esse bloco dentro do card da escala passada, após as informações do card (participantes, músicas) mas antes do fechamento do card.
  </action>

  <verify>
    <automated>php -l admin/escalas.php</automated>
  </verify>

  <acceptance_criteria>
    - `php -l admin/escalas.php` retorna "No syntax errors detected"
    - Arquivo contém `registrar_faltas.php?id=`
    - Link só aparece dentro de `$_SESSION['user_role'] === 'admin'` check
    - Link usa cor laranja (`--orange-500` / `#f97316`) — CTA color do design system
  </acceptance_criteria>

  <done>Botão "Registrar Faltas" adicionado nas escalas passadas em admin/escalas.php (admin only).</done>
</task>

<task type="auto">
  <name>Task 3: Criar admin/registrar_faltas.php — tela de registro de presenças/faltas</name>
  <files>admin/registrar_faltas.php</files>

  <read_first>
    - admin/escala_detalhe.php — ver padrão completo de página admin: includes, renderAppHeader, HTML structure, CSS pattern
    - includes/auth.php — ver checkAdmin() e como a sessão é usada
  </read_first>

  <action>
Criar `admin/registrar_faltas.php` com o seguinte conteúdo completo:

```php
<?php
// admin/registrar_faltas.php — Registro de presenças e faltas pós-culto (admin only)
require_once '../includes/db.php';
require_once '../includes/layout.php';
checkAdmin();

$scheduleId = (int)($_GET['id'] ?? 0);
if (!$scheduleId) {
    header('Location: escalas.php');
    exit;
}

// Buscar escala — deve ser do passado
$stmtSched = $pdo->prepare("SELECT * FROM schedules WHERE id = ?");
$stmtSched->execute([$scheduleId]);
$schedule = $stmtSched->fetch(PDO::FETCH_ASSOC);

if (!$schedule) {
    header('Location: escalas.php');
    exit;
}

// Garantir que é uma escala passada
if ($schedule['event_date'] >= date('Y-m-d')) {
    header('Location: escalas.php?erro=escala_futura');
    exit;
}

// Buscar participantes da escala
$stmtUsers = $pdo->prepare("
    SELECT su.user_id, su.status, su.absence_note, su.instrument as assigned_instrument,
           u.name, u.instrument as default_instrument, u.avatar_color
    FROM schedule_users su
    JOIN users u ON su.user_id = u.id
    WHERE su.schedule_id = ?
    ORDER BY u.name ASC
");
$stmtUsers->execute([$scheduleId]);
$participants = $stmtUsers->fetchAll(PDO::FETCH_ASSOC);

if (empty($participants)) {
    header('Location: escala_detalhe.php?id=' . $scheduleId);
    exit;
}

$eventDate = new DateTime($schedule['event_date']);
$dateFormatted = $eventDate->format('d/m/Y');
$dayName = ['Domingo','Segunda','Terça','Quarta','Quinta','Sexta','Sábado'][$eventDate->format('w')];

renderAppHeader('Registrar Faltas', 'escalas.php');
?>
<link rel="stylesheet" href="../assets/css/pages/registrar_faltas.css">

<div class="page-container">

  <div class="schedule-info-card pib-card">
    <div class="schedule-info-header">
      <span class="event-type-badge"><?= htmlspecialchars($schedule['event_type']) ?></span>
      <span class="event-date"><?= $dayName ?>, <?= $dateFormatted ?></span>
    </div>
    <p class="schedule-info-hint">Marque o status de presença de cada participante. Isso alimenta o histórico pastoral da equipe.</p>
  </div>

  <div id="feedback-msg" class="feedback-msg" style="display:none;"></div>

  <div class="participants-list" id="participants-list">
    <?php foreach ($participants as $p): ?>
    <?php
      $statusClass = in_array($p['status'], ['confirmed','absent','absent_justified']) ? $p['status'] : 'pending';
      $instrument = $p['assigned_instrument'] ?: $p['default_instrument'];
    ?>
    <div class="participant-card pib-card" data-user-id="<?= (int)$p['user_id'] ?>">
      <div class="participant-info">
        <div class="participant-avatar" style="background:<?= htmlspecialchars($p['avatar_color'] ?: '#3B82F6') ?>">
          <?= mb_substr(htmlspecialchars($p['name']), 0, 1) ?>
        </div>
        <div class="participant-details">
          <span class="participant-name"><?= htmlspecialchars($p['name']) ?></span>
          <?php if ($instrument): ?>
          <span class="participant-instrument"><?= htmlspecialchars($instrument) ?></span>
          <?php endif; ?>
        </div>
      </div>

      <div class="status-toggle" role="group" aria-label="Status de <?= htmlspecialchars($p['name']) ?>">
        <button type="button"
                class="toggle-btn <?= ($statusClass === 'confirmed' || $statusClass === 'pending') ? 'active' : '' ?>"
                data-status="confirmed"
                aria-pressed="<?= ($statusClass === 'confirmed' || $statusClass === 'pending') ? 'true' : 'false' ?>">
          ✅ Presente
        </button>
        <button type="button"
                class="toggle-btn <?= $statusClass === 'absent' ? 'active absent' : '' ?>"
                data-status="absent"
                aria-pressed="<?= $statusClass === 'absent' ? 'true' : 'false' ?>">
          ❌ Faltou
        </button>
        <button type="button"
                class="toggle-btn <?= $statusClass === 'absent_justified' ? 'active justified' : '' ?>"
                data-status="absent_justified"
                aria-pressed="<?= $statusClass === 'absent_justified' ? 'true' : 'false' ?>">
          ⚠️ Justificou
        </button>
      </div>

      <div class="absence-note-field" style="<?= in_array($statusClass, ['absent','absent_justified']) ? '' : 'display:none' ?>">
        <input type="text"
               class="absence-note-input"
               placeholder="Motivo (opcional, só você vê)"
               maxlength="255"
               value="<?= htmlspecialchars($p['absence_note'] ?? '') ?>">
      </div>
    </div>
    <?php endforeach; ?>
  </div>

  <div class="save-footer">
    <button id="btn-save" class="btn-save-primary" onclick="saveAbsences()">
      Salvar Registro
    </button>
    <a href="escalas.php" class="btn-cancel">Cancelar</a>
  </div>

</div>

<script>
const SCHEDULE_ID = <?= (int)$scheduleId ?>;

// Toggle de status por card
document.querySelectorAll('.status-toggle').forEach(group => {
  group.addEventListener('click', e => {
    const btn = e.target.closest('.toggle-btn');
    if (!btn) return;

    const card = btn.closest('.participant-card');
    const newStatus = btn.dataset.status;

    // Atualizar botões ativos
    group.querySelectorAll('.toggle-btn').forEach(b => {
      b.classList.remove('active', 'absent', 'justified');
      b.setAttribute('aria-pressed', 'false');
    });
    btn.classList.add('active');
    if (newStatus === 'absent') btn.classList.add('absent');
    if (newStatus === 'absent_justified') btn.classList.add('justified');
    btn.setAttribute('aria-pressed', 'true');

    // Mostrar/ocultar campo de nota
    const noteField = card.querySelector('.absence-note-field');
    if (newStatus === 'absent' || newStatus === 'absent_justified') {
      noteField.style.display = '';
      noteField.querySelector('input').focus();
    } else {
      noteField.style.display = 'none';
    }
  });
});

async function saveAbsences() {
  const btn = document.getElementById('btn-save');
  btn.disabled = true;
  btn.textContent = 'Salvando...';

  const participants = [];
  document.querySelectorAll('.participant-card').forEach(card => {
    const userId = parseInt(card.dataset.userId);
    const activeBtn = card.querySelector('.toggle-btn.active');
    const status = activeBtn ? activeBtn.dataset.status : 'confirmed';
    const note = card.querySelector('.absence-note-input')?.value?.trim() || null;
    participants.push({ user_id: userId, status, note });
  });

  try {
    const res = await fetch('../api/save_absences.php', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ schedule_id: SCHEDULE_ID, participants })
    });
    const data = await res.json();

    const msg = document.getElementById('feedback-msg');
    if (data.success) {
      msg.className = 'feedback-msg success';
      msg.textContent = 'Registro salvo com sucesso!';
      msg.style.display = '';
      btn.textContent = 'Salvo ✓';
      setTimeout(() => { window.location.href = 'escalas.php'; }, 1200);
    } else {
      msg.className = 'feedback-msg error';
      msg.textContent = data.message || 'Erro ao salvar.';
      msg.style.display = '';
      btn.disabled = false;
      btn.textContent = 'Salvar Registro';
    }
  } catch (err) {
    const msg = document.getElementById('feedback-msg');
    msg.className = 'feedback-msg error';
    msg.textContent = 'Erro de conexão. Tente novamente.';
    msg.style.display = '';
    btn.disabled = false;
    btn.textContent = 'Salvar Registro';
  }
}
</script>
```
  </action>

  <verify>
    <automated>php -l admin/registrar_faltas.php</automated>
  </verify>

  <acceptance_criteria>
    - `php -l admin/registrar_faltas.php` retorna "No syntax errors detected"
    - Contém `checkAdmin()` no início
    - Valida que a escala é do passado (`event_date < CURDATE`) — redireciona se futura
    - Exibe lista de participantes com nome, instrumento e avatar colorido
    - Cada participante tem 3 botões de toggle: Presente, Faltou, Justificou
    - Campo de nota de ausência aparece ao clicar "Faltou" ou "Justificou"
    - Botão "Salvar Registro" chama `saveAbsences()` que faz fetch para `api/save_absences.php`
    - Após salvar com sucesso, redireciona para `escalas.php` após 1.2s
    - SCHEDULE_ID é injetado server-side como integer (sem XSS)
    - Todos os valores de HTML de dados do usuário usam `htmlspecialchars()`
  </acceptance_criteria>

  <done>
Tela admin/registrar_faltas.php criada com lista de participantes, toggles de 3 estados e campo de nota opcional.
  </done>
</task>

<task type="auto">
  <name>Task 4: Criar assets/css/pages/registrar_faltas.css</name>
  <files>assets/css/pages/registrar_faltas.css</files>

  <read_first>
    - assets/css/core/variables.css — ver variáveis CSS disponíveis (--blue-500, --orange-500, --red-500, etc.)
    - assets/css/pages/escalas.css — ver padrão de CSS de página de escalas (cards, layout mobile-first)
  </read_first>

  <action>
Criar `assets/css/pages/registrar_faltas.css` com estilos mobile-first:

```css
/* registrar_faltas.css — Tela de registro de presenças/faltas */

.page-container {
  padding: 16px;
  max-width: 600px;
  margin: 0 auto;
  padding-bottom: 100px; /* espaço para o save-footer */
}

.schedule-info-card {
  padding: 16px;
  margin-bottom: 16px;
}

.schedule-info-header {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 8px;
  flex-wrap: wrap;
}

.event-type-badge {
  background: var(--blue-500, #3b82f6);
  color: #fff;
  font-size: 0.75rem;
  font-weight: 700;
  padding: 3px 10px;
  border-radius: 20px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.event-date {
  font-size: 0.9rem;
  font-weight: 600;
  color: var(--gray-700, #374151);
}

.schedule-info-hint {
  font-size: 0.8rem;
  color: var(--gray-500, #6b7280);
  margin: 0;
  line-height: 1.4;
}

/* Feedback */
.feedback-msg {
  padding: 12px 16px;
  border-radius: 10px;
  font-size: 0.875rem;
  font-weight: 600;
  margin-bottom: 12px;
}
.feedback-msg.success {
  background: #d1fae5;
  color: #065f46;
  border: 1px solid #6ee7b7;
}
.feedback-msg.error {
  background: #fee2e2;
  color: #991b1b;
  border: 1px solid #fca5a5;
}

/* Participant Cards */
.participants-list {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.participant-card {
  padding: 14px;
}

.participant-info {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-bottom: 12px;
}

.participant-avatar {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #fff;
  font-size: 1.1rem;
  font-weight: 700;
  flex-shrink: 0;
}

.participant-details {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.participant-name {
  font-size: 0.95rem;
  font-weight: 600;
  color: var(--gray-900, #111827);
}

.participant-instrument {
  font-size: 0.75rem;
  color: var(--gray-500, #6b7280);
}

/* Status Toggle */
.status-toggle {
  display: flex;
  gap: 6px;
  flex-wrap: wrap;
}

.toggle-btn {
  flex: 1;
  min-width: 90px;
  padding: 8px 6px;
  border: 1.5px solid var(--gray-300, #d1d5db);
  background: var(--gray-50, #f9fafb);
  color: var(--gray-600, #4b5563);
  border-radius: 8px;
  font-size: 0.78rem;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.15s ease;
  min-height: 44px;
  text-align: center;
}

.toggle-btn.active {
  background: var(--green-100, #d1fae5);
  border-color: var(--green-500, #10b981);
  color: var(--green-700, #065f46);
}

.toggle-btn.active.absent {
  background: var(--red-100, #fee2e2);
  border-color: var(--red-500, #ef4444);
  color: var(--red-700, #b91c1c);
}

.toggle-btn.active.justified {
  background: #fef3c7;
  border-color: #f59e0b;
  color: #92400e;
}

/* Absence Note */
.absence-note-field {
  margin-top: 10px;
}

.absence-note-input {
  width: 100%;
  padding: 10px 12px;
  border: 1px solid var(--gray-300, #d1d5db);
  border-radius: 8px;
  font-size: 0.875rem;
  color: var(--gray-800, #1f2937);
  background: var(--gray-50, #f9fafb);
  box-sizing: border-box;
  min-height: 44px;
}

.absence-note-input:focus {
  outline: none;
  border-color: var(--blue-500, #3b82f6);
  box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
}

/* Save Footer */
.save-footer {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  background: #fff;
  border-top: 1px solid var(--gray-200, #e5e7eb);
  padding: 12px 16px;
  padding-bottom: calc(12px + env(safe-area-inset-bottom, 0px));
  display: flex;
  gap: 10px;
  align-items: center;
  z-index: 100;
}

.btn-save-primary {
  flex: 1;
  padding: 14px;
  background: var(--blue-500, #3b82f6);
  color: #fff;
  border: none;
  border-radius: 10px;
  font-size: 0.95rem;
  font-weight: 700;
  cursor: pointer;
  min-height: 48px;
  transition: background 0.15s;
}

.btn-save-primary:hover:not(:disabled) {
  background: var(--blue-600, #2563eb);
}

.btn-save-primary:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}

.btn-cancel {
  padding: 14px 20px;
  border: 1.5px solid var(--gray-300, #d1d5db);
  border-radius: 10px;
  font-size: 0.875rem;
  font-weight: 600;
  color: var(--gray-600, #4b5563);
  text-decoration: none;
  min-height: 48px;
  display: flex;
  align-items: center;
}
```
  </action>

  <verify>
    <automated>php -r "echo file_exists('assets/css/pages/registrar_faltas.css') ? 'OK' : 'MISSING';"</automated>
  </verify>

  <acceptance_criteria>
    - Arquivo `assets/css/pages/registrar_faltas.css` existe
    - Contém `.toggle-btn.active` (presente — verde)
    - Contém `.toggle-btn.active.absent` (faltou — vermelho)
    - Contém `.toggle-btn.active.justified` (justificou — âmbar)
    - Contém `.save-footer` com `position: fixed` e `safe-area-inset-bottom` para iOS
    - Todos os tamanhos mínimos de toque: `.toggle-btn` e `.btn-save-primary` têm `min-height: 44px+`
  </acceptance_criteria>

  <done>CSS assets/css/pages/registrar_faltas.css criado com estilos dos toggles e footer fixo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → admin/registrar_faltas.php | Apenas admin acessa; escala deve ser do passado |
| JavaScript → api/save_absences.php | Dados do formulário enviados via fetch POST JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04A-01 | Elevation | admin/registrar_faltas.php | mitigate | `checkAdmin()` no início — redireciona não-admin |
| T-04A-02 | Tampering | event_date check | mitigate | `event_date >= date('Y-m-d')` redireciona escalas futuras |
| T-04A-03 | XSS | HTML output | mitigate | `htmlspecialchars()` em todos os valores de dados do usuário exibidos |
| T-04A-04 | XSS | SCHEDULE_ID no JS | mitigate | Injetado como `(int)$scheduleId` — só integer, sem string |
</threat_model>

<verification>
1. `php -l admin/registrar_faltas.php` — sem erros de sintaxe
2. `php -l admin/escalas.php` — sem erros de sintaxe após modificação
3. `php -r "echo file_exists('database/migrations/004_schedule_users_absences.sql') ? 'OK' : 'MISSING';"` — OK
4. `php -r "echo file_exists('assets/css/pages/registrar_faltas.css') ? 'OK' : 'MISSING';"` — OK
5. `grep "registrar_faltas" admin/escalas.php` — link presente
6. `grep "checkAdmin" admin/registrar_faltas.php` — presente
7. `grep "event_date >= date" admin/registrar_faltas.php` — guard de escala futura presente
</verification>

<success_criteria>
- Migration 004 criada com absent/absent_justified no ENUM e absence_note TEXT NULL
- Link "Registrar Faltas" visível nos cards de escalas passadas em admin/escalas.php (admin only)
- admin/registrar_faltas.php sem erros de sintaxe PHP
- Página exibe lista de participantes com toggles de 3 estados
- Campo de nota aparece ao selecionar "Faltou" ou "Justificou"
- Botão salva via fetch e redireciona para escalas.php
- CSS mobile-first com toggles coloridos e footer fixo com safe-area-inset
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-registrar-faltas/04-01-SUMMARY.md`
</output>
