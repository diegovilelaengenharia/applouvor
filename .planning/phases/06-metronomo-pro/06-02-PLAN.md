---
phase: 06-metronomo-pro
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01-PLAN.md"
files_modified:
  - includes/dashboard_cards.php
  - admin/index.php
  - admin/musica_detalhe.php
  - sw.js
autonomous: true
requirements:
  - MET-03

must_haves:
  truths:
    - "includes/dashboard_cards.php tem card 'metronomo' na categoria gestao"
    - "admin/index.php inclui 'metronomo' no array $groupedCards['gestao']"
    - "admin/musica_detalhe.php tem link para metronomo.php?bpm=X quando $song['bpm'] existe"
    - "sw.js inclui 'admin/metronomo.php' em urlsToCache"
  artifacts:
    - path: "includes/dashboard_cards.php"
      provides: "Card metrônomo no dashboard"
      contains: "metronomo"
    - path: "sw.js"
      provides: "Cache offline do metrônomo"
      contains: "metronomo"
  key_links:
    - from: "includes/dashboard_cards.php"
      to: "admin/metronomo.php"
      via: "card url"
      pattern: "metronomo"
    - from: "admin/musica_detalhe.php"
      to: "admin/metronomo.php"
      via: "link com ?bpm="
      pattern: "metronomo.php?bpm"
---

<objective>
Integrar `admin/metronomo.php` ao ecossistema do app: card no dashboard, link de "Abrir no metrônomo" em musica_detalhe.php (quando bpm existe), e cache no SW para uso offline.

Purpose: MET-03 — metrônomo acessível do dashboard e de dentro da página da música; disponível offline no ensaio.
Output: 4 arquivos modificados para integração completa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-metronomo-pro/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar card metrônomo em dashboard_cards.php</name>
  <files>includes/dashboard_cards.php</files>

  <read_first>
    - includes/dashboard_cards.php — localizar o final do array em getAllAvailableCards(), antes de `];`
  </read_first>

  <action>
Localizar no arquivo o final do array de cards, antes do fechamento `];` da função `getAllAvailableCards()`.

Adicionar card 'metronomo' no bloco de gestão (antes de `// ===== ESPIRITUALIDADE`):

```php
        'metronomo' => [
            'id' => 'metronomo',
            'title' => 'Metrônomo',
            'icon' => 'timer',
            'category' => 'gestao',
            'category_name' => 'Gestão',
            'color' => '#2563EB',
            'bg' => '#DBEAFE',
            'url' => 'metronomo.php',
            'admin_only' => false
        ],
```
  </action>

  <verify>
    <automated>php -l includes/dashboard_cards.php</automated>
  </verify>

  <acceptance_criteria>
    - `php -l includes/dashboard_cards.php` sem erros
    - Contém `'metronomo'`
    - Contém `metronomo.php`
  </acceptance_criteria>

  <done>Card metrônomo adicionado em dashboard_cards.php.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar 'metronomo' na lista do dashboard em admin/index.php</name>
  <files>admin/index.php</files>

  <read_first>
    - admin/index.php — localizar o array $groupedCards (linha ~58-62) e o array 'gestao'
  </read_first>

  <action>
Localizar o array `$groupedCards` em `admin/index.php`:

```php
$groupedCards = [
    'gestao' => ['escalas', 'repertorio', 'membros', 'agenda', 'historico'],
```

Substituir por (adicionar 'metronomo'):

```php
$groupedCards = [
    'gestao' => ['escalas', 'repertorio', 'membros', 'agenda', 'historico', 'metronomo'],
```
  </action>

  <verify>
    <automated>php -l admin/index.php</automated>
  </verify>

  <acceptance_criteria>
    - `php -l admin/index.php` sem erros
    - Contém `'metronomo'` no array $groupedCards
  </acceptance_criteria>

  <done>'metronomo' adicionado no dashboard.</done>
</task>

<task type="auto">
  <name>Task 3: Adicionar link "Metrônomo" em musica_detalhe.php quando BPM existe</name>
  <files>admin/musica_detalhe.php</files>

  <read_first>
    - admin/musica_detalhe.php — localizar o TAB 1 (Visão Geral) onde BPM é exibido — procurar por `$song['bpm']` e a área de info-grid-row / stats
  </read_first>

  <action>
Localizar em `admin/musica_detalhe.php` onde fica o display do BPM (dentro do TAB 1). Após o bloco dos cards de Tom/BPM/Duração (info-grid-row), adicionar link para o metrônomo com o BPM da música:

```php
<?php if (!empty($song['bpm'])): ?>
<div style="margin-top:12px;">
    <a href="metronomo.php?bpm=<?= (int)$song['bpm'] ?>"
       style="display:inline-flex;align-items:center;gap:8px;font-size:.85rem;font-weight:700;
              color:#3b82f6;text-decoration:none;padding:10px 16px;
              background:#eff6ff;border:1.5px solid #3b82f6;border-radius:10px;">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24"
             fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="14" r="8"/><path d="M12 6V2"/><path d="M15.5 3.5 13 6"/>
            <path d="M9 14l2.5-2.5"/>
        </svg>
        Abrir no metrônomo — <?= (int)$song['bpm'] ?> BPM
    </a>
</div>
<?php endif; ?>
```

Inserir após o bloco `.info-grid-row` (Tom, BPM, Duração) dentro do TAB 1 (tab-info / panel-card).
  </action>

  <verify>
    <automated>php -l admin/musica_detalhe.php</automated>
  </verify>

  <acceptance_criteria>
    - `php -l admin/musica_detalhe.php` sem erros
    - Contém `metronomo.php?bpm=`
    - BPM injetado como `(int)$song['bpm']`
  </acceptance_criteria>

  <done>Link "Abrir no metrônomo" adicionado em musica_detalhe.php quando BPM existe.</done>
</task>

<task type="auto">
  <name>Task 4: Adicionar metronomo.php ao cache do SW</name>
  <files>sw.js</files>

  <read_first>
    - sw.js — localizar o array `urlsToCache` (linhas 2-9) para adicionar a URL
  </read_first>

  <action>
Localizar em `sw.js` o array `urlsToCache` e adicionar a URL do metrônomo:

Substituir:
```js
const urlsToCache = [
  '/',
  '/index.php',
  '/assets/css/style.css',
  '/assets/images/logo-black.png',
  '/assets/images/logo-white.png',
  'https://unpkg.com/lucide@latest'
];
```

Por:
```js
const urlsToCache = [
  '/',
  '/index.php',
  '/assets/css/style.css',
  '/assets/images/logo-black.png',
  '/assets/images/logo-white.png',
  '/admin/metronomo.php',
  'https://unpkg.com/lucide@latest'
];
```

Também atualizar o `CACHE_NAME` para forçar atualização do cache:

Substituir:
```js
const CACHE_NAME = 'louvor-pib-v2.2.0';
```

Por:
```js
const CACHE_NAME = 'louvor-pib-v2.3.0';
```
  </action>

  <verify>
    <automated>node --check sw.js 2>&1 || echo "check done"</automated>
  </verify>

  <acceptance_criteria>
    - Contém `/admin/metronomo.php` em urlsToCache
    - Contém `louvor-pib-v2.3.0` (versão atualizada)
  </acceptance_criteria>

  <done>metronomo.php adicionado ao cache do SW e versão incrementada para 2.3.0.</done>
</task>

</tasks>

<threat_model>
## STRIDE

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-06B-01 | Information Disclosure | Link BPM em musica_detalhe | accept | BPM é dado público dentro do app |
</threat_model>

<verification>
1. `php -l includes/dashboard_cards.php` + `php -l admin/index.php` + `php -l admin/musica_detalhe.php` — sem erros
2. `grep "metronomo" includes/dashboard_cards.php admin/index.php admin/musica_detalhe.php sw.js` — presentes em todos
3. `grep "v2.3.0" sw.js` — versão atualizada
</verification>

<success_criteria>
- Card metrônomo aparece no dashboard para todos os usuários
- Link "Abrir no metrônomo" aparece na página da música quando BPM está cadastrado
- SW cacheia metronomo.php para uso offline
- Sem erros de sintaxe PHP/JS
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-metronomo-pro/06-02-SUMMARY.md`
</output>
