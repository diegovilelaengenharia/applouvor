---
phase: 05-musica-modernizada
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - admin/repertorio.php
autonomous: true
requirements:
  - MUS-05

must_haves:
  truths:
    - "admin/repertorio.php mostra 'Última: dd/mm/aa' abaixo do artista em cada card de música"
    - "Músicas nunca usadas mostram 'Nunca tocada' em tom apagado"
    - "Queries do tab=musicas incluem LEFT JOIN schedule_songs + schedules para MAX(event_date)"
    - "Todos os três paths da query (tagId, tone, busca normal) incluem last_played"
    - "GROUP BY s.id garante uma linha por música mesmo com múltiplas escalas"
  artifacts:
    - path: "admin/repertorio.php"
      provides: "Listagem de músicas com última data tocada"
      contains: "last_played"
  key_links:
    - from: "admin/repertorio.php"
      to: "songs + schedule_songs + schedules (tables)"
      via: "LEFT JOIN para MAX(event_date)"
      pattern: "last_played"
---

<objective>
Adicionar badge "Última: dd/mm/aa" em cada card de música no tab Músicas de `admin/repertorio.php`, obtido via LEFT JOIN com schedule_songs → schedules para encontrar a data máxima de uso de cada música.

Purpose: MUS-05 — músicos e líder precisam saber quando cada música foi tocada pela última vez para planejar repertório com variedade.
Output: `admin/repertorio.php` com data "última vez tocada" visível em cada card.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-musica-modernizada/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar last_played nas queries e exibir no card</name>
  <files>admin/repertorio.php</files>

  <read_first>
    - admin/repertorio.php — ver os três branches da query do tab=musicas (linhas 107-136) e o bloco do card de música (linhas 186-244)
  </read_first>

  <action>
**Parte A: Query por Tag** — localizar a query que filtra por `$tagId` e adicionar `MAX(sch.event_date) as last_played`, LEFT JOINs e GROUP BY:

Substituir:
```php
                $sql = "
                    SELECT s.* 
                    FROM songs s 
                    JOIN song_tags st ON s.id = st.song_id 
                    WHERE st.tag_id = :tagId ";
                 if (!empty($search)) { $sql .= " AND (s.title LIKE :q OR s.artist LIKE :q) "; }
                $sql .= " ORDER BY s.title ASC LIMIT 50";
```

Por:
```php
                $sql = "
                    SELECT s.*, MAX(sch.event_date) as last_played
                    FROM songs s
                    JOIN song_tags st ON s.id = st.song_id
                    LEFT JOIN schedule_songs ss ON ss.song_id = s.id
                    LEFT JOIN schedules sch ON sch.id = ss.schedule_id
                    WHERE st.tag_id = :tagId ";
                 if (!empty($search)) { $sql .= " AND (s.title LIKE :q OR s.artist LIKE :q) "; }
                $sql .= " GROUP BY s.id ORDER BY s.title ASC LIMIT 50";
```

**Parte B: Query por Tom** — localizar a query que filtra por `$tone`:

Substituir:
```php
                $sql = "SELECT * FROM songs WHERE tone = :tone";
                if (!empty($search)) { $sql .= " AND (title LIKE :q OR artist LIKE :q) "; }
                $sql .= " ORDER BY title ASC LIMIT 50";
```

Por:
```php
                $sql = "SELECT s.*, MAX(sch.event_date) as last_played
                        FROM songs s
                        LEFT JOIN schedule_songs ss ON ss.song_id = s.id
                        LEFT JOIN schedules sch ON sch.id = ss.schedule_id
                        WHERE s.tone = :tone";
                if (!empty($search)) { $sql .= " AND (s.title LIKE :q OR s.artist LIKE :q) "; }
                $sql .= " GROUP BY s.id ORDER BY s.title ASC LIMIT 50";
```

Também atualizar os params para usar prefixo `s.`:
```php
                $params = ['tone' => $tone];
                if (!empty($search)) { $params['q'] = "%$search%"; }
                $stmt->execute($params);
```
(sem alteração nos params — só a query muda)

**Parte C: Query normal** — localizar a busca padrão sem filtros:

Substituir:
```php
                $sql = "SELECT * FROM songs WHERE title LIKE :q OR artist LIKE :q ORDER BY title ASC LIMIT 50";
                $stmt = $pdo->prepare($sql);
                $stmt->execute(['q' => "%$search%"]);
```

Por:
```php
                $sql = "SELECT s.*, MAX(sch.event_date) as last_played
                        FROM songs s
                        LEFT JOIN schedule_songs ss ON ss.song_id = s.id
                        LEFT JOIN schedules sch ON sch.id = ss.schedule_id
                        WHERE s.title LIKE :q OR s.artist LIKE :q
                        GROUP BY s.id
                        ORDER BY s.title ASC LIMIT 50";
                $stmt = $pdo->prepare($sql);
                $stmt->execute(['q' => "%$search%"]);
```

**Parte D: Exibir last_played no card** — localizar o bloco do artista no card de música:

```php
                                    <p style="margin: 0; font-size: 0.75rem; color: var(--color-text-muted); font-weight: 600;"><?= htmlspecialchars($song['artist']) ?></p>
```

Substituir por:
```php
                                    <p style="margin: 0; font-size: 0.75rem; color: var(--color-text-muted); font-weight: 600;"><?= htmlspecialchars($song['artist']) ?></p>
                                    <?php if (!empty($song['last_played'])): ?>
                                    <p style="margin: 0; font-size: 0.68rem; color: var(--color-text-muted); opacity: 0.7;">
                                        Última: <?= (new DateTime($song['last_played']))->format('d/m/y') ?>
                                    </p>
                                    <?php else: ?>
                                    <p style="margin: 0; font-size: 0.68rem; color: var(--color-text-muted); opacity: 0.4;">Nunca tocada</p>
                                    <?php endif; ?>
```
  </action>

  <verify>
    <automated>php -l admin/repertorio.php</automated>
  </verify>

  <acceptance_criteria>
    - `php -l admin/repertorio.php` sem erros
    - Contém `last_played` (campo calculado)
    - Contém `MAX(sch.event_date)`
    - Contém `LEFT JOIN schedule_songs`
    - Contém `GROUP BY s.id`
    - Contém `Nunca tocada` (estado vazio)
    - Contém `format('d/m/y')` (formatação da data)
    - Todos os três paths de query foram atualizados
  </acceptance_criteria>

  <done>repertorio.php atualizado com "última vez tocada" em cada card de música.</done>
</task>

</tasks>

<threat_model>
## STRIDE

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05D-01 | Information Disclosure | last_played no card | accept | Data pública dentro do app — todos os usuários logados já acessam escalas |
</threat_model>

<verification>
1. `php -l admin/repertorio.php` — sem erros
2. `grep "last_played\|MAX(sch" admin/repertorio.php` — presentes
3. `grep "GROUP BY s.id" admin/repertorio.php` — presente (evita duplicatas)
</verification>

<success_criteria>
- Cards de músicas mostram "Última: dd/mm/aa" quando já foram tocadas
- Cards mostram "Nunca tocada" (opaco) quando sem histórico
- Sem erros de sintaxe PHP
- Sem regressão nos filtros por tag, tom e busca
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-musica-modernizada/05-04-SUMMARY.md`
</output>
