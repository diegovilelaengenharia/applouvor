---
phase: 05-musica-modernizada
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - admin/dashboard_data.php
  - admin/index.php
autonomous: true
requirements:
  - MUS-03

must_haves:
  truths:
    - "dashboard_data.php conta sugestões pendentes da tabela song_suggestions (não avisos)"
    - "Variável $pendingSuggestions disponível no template do dashboard"
    - "Badge visível no dashboard para admin quando há sugestões pendentes (link para sugestoes_musicas.php)"
    - "Badge invisível para músicos (role !== admin)"
  artifacts:
    - path: "admin/dashboard_data.php"
      provides: "Contagem de sugestões pendentes"
      contains: "song_suggestions"
    - path: "admin/index.php"
      provides: "Badge de sugestões no dashboard"
      contains: "pendingSuggestions"
  key_links:
    - from: "admin/index.php"
      to: "admin/sugestoes_musicas.php"
      via: "link do badge"
      pattern: "sugestoes_musicas"
---

<objective>
Corrigir a contagem de sugestões pendentes no dashboard — atualmente usa a tabela `avisos`, deve usar `song_suggestions`. Adicionar badge visível para admin no dashboard quando há sugestões pendentes.

Purpose: MUS-03 — o fluxo de sugestão já existe (sugerir_musica.php + sugestoes_musicas.php), falta apenas o sinal de notificação para o líder no dashboard.
Output: Badge "X sugestões" no dashboard admin linkando para sugestoes_musicas.php.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-musica-modernizada/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir contagem em dashboard_data.php</name>
  <files>admin/dashboard_data.php</files>

  <read_first>
    - admin/dashboard_data.php — ler completo para localizar onde $historicoData é setado e a query de sugestoes_count errada
  </read_first>

  <action>
Localizar no arquivo a linha que conta sugestões via `avisos WHERE category = 'Sugestão'` e substituir por query correta na tabela `song_suggestions`:

Substituir:
```php
$historicoData['sugestoes_count'] = $pdo->query("SELECT COUNT(*) FROM avisos WHERE category = 'Sugestão'")->fetchColumn();
```

Por:
```php
$historicoData['sugestoes_count'] = $pdo->query("SELECT COUNT(*) FROM song_suggestions WHERE status = 'pending'")->fetchColumn();
```

Também adicionar `$pendingSuggestions` como variável de topo para uso no template — adicionar logo após o bloco de carga do `$historicoData`:

```php
// Sugestões pendentes (para badge no dashboard — admin only)
$pendingSuggestions = ($userRole === 'admin') ? (int)$historicoData['sugestoes_count'] : 0;
```
  </action>

  <verify>
    <automated>php -l admin/dashboard_data.php</automated>
  </verify>

  <acceptance_criteria>
    - `php -l admin/dashboard_data.php` sem erros
    - Contém `song_suggestions WHERE status = 'pending'`
    - Contém `$pendingSuggestions`
    - Não contém mais `avisos WHERE category = 'Sugestão'` para contagem de sugestões musicais
  </acceptance_criteria>

  <done>dashboard_data.php corrigido — conta song_suggestions pendentes.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar badge de sugestões no dashboard (admin only)</name>
  <files>admin/index.php</files>

  <read_first>
    - admin/index.php — ler o arquivo completo para entender onde os cards do dashboard são renderizados e onde inserir o badge
    - admin/dashboard_render.php — verificar se existe função de renderização que exibe cards de repertório/gestão
  </read_first>

  <action>
No arquivo `admin/index.php`, localizar a área que exibe o card/link de "Repertório" ou onde os cards de gestão são exibidos.

Adicionar badge de sugestões pendentes após/dentro do card de Repertório (visível apenas para admin):

```php
<?php if ($pendingSuggestions > 0 && $_SESSION['user_role'] === 'admin'): ?>
<a href="sugestoes_musicas.php" style="
    display:flex;align-items:center;gap:10px;
    padding:12px 16px;margin:0 16px 12px;
    background:#fff7ed;border:1.5px solid #f97316;border-radius:12px;
    text-decoration:none;color:#92400e;">
    <span style="background:#f97316;color:#fff;font-size:.75rem;font-weight:800;
                 padding:3px 9px;border-radius:20px;">
        <?= $pendingSuggestions ?>
    </span>
    <span style="font-size:.875rem;font-weight:600;">
        sugestão<?= $pendingSuggestions > 1 ? 'ões' : '' ?> de música pendente<?= $pendingSuggestions > 1 ? 's' : '' ?>
    </span>
    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24"
         fill="none" stroke="#f97316" stroke-width="2" style="margin-left:auto;">
        <polyline points="9 18 15 12 9 6"/>
    </svg>
</a>
<?php endif; ?>
```

Inserir esse bloco em local visível do dashboard — preferencialmente após o cabeçalho/saudação e antes dos grupos de cards, ou dentro da seção de gestão.
  </action>

  <verify>
    <automated>php -l admin/index.php</automated>
  </verify>

  <acceptance_criteria>
    - `php -l admin/index.php` sem erros
    - Contém `$pendingSuggestions`
    - Contém link para `sugestoes_musicas.php`
    - Badge só renderiza quando `$pendingSuggestions > 0 && user_role === 'admin'`
  </acceptance_criteria>

  <done>Badge de sugestões pendentes adicionado no dashboard (admin only).</done>
</task>

</tasks>

<threat_model>
## STRIDE

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05B-01 | Information Disclosure | Badge para não-admin | mitigate | `$pendingSuggestions = ($userRole === 'admin') ? ... : 0` — músicos vêem 0 |
</threat_model>

<verification>
1. `php -l admin/dashboard_data.php` + `php -l admin/index.php` — sem erros
2. `grep "song_suggestions" admin/dashboard_data.php` — presente
3. `grep "pendingSuggestions" admin/index.php` — presente
</verification>

<success_criteria>
- Contagem usa tabela song_suggestions (não avisos)
- Badge aparece no dashboard quando há sugestões pendentes (admin only)
- Link do badge vai para sugestoes_musicas.php
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-musica-modernizada/05-02-SUMMARY.md`
</output>
