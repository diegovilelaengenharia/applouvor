# site/.env — SÓ para desenvolvimento local (XAMPP). Nunca commitar o .env real (gitignored).
# Em produção (Hostinger), estas mesmas chaves são cadastradas no PAINEL como variáveis de
# ambiente do PHP — nunca em arquivo. Ver .governanca/fases/FASE-00-PLANO.md.

DB_HOST=localhost
DB_NAME=pibo_louvor
DB_USER=root
DB_PASS=

APP_ENV=local
APP_DEBUG=true
